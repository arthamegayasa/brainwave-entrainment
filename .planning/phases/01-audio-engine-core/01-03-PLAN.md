---
phase: 01-audio-engine-core
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/audio/layers/isochronic.ts
  - src/audio/layers/monaural.ts
  - src/audio/engine.ts
  - tests/audio/isochronic.test.ts
  - tests/audio/monaural.test.ts
autonomous: true
requirements: [ENG-02, ENG-03]
must_haves:
  truths:
    - "User menekan tombol Isochronic dan mendengar tone yang berdenyut halus 10x per detik — tanpa click di tiap pulse"
    - "User menekan tombol Monaural dan mendengar beat yang sama di kedua telinga (bekerja di speaker)"
    - "Envelope pulse isochronic menghasilkan TEPAT beat Hz pulse per detik (bukan 2x — bug curve non-monotonic tertangkap test)"
  artifacts:
    - path: "src/audio/layers/isochronic.ts"
      provides: "IsochronicLayer: carrier osc → envGain(base 0) ← WaveShaper(curve monotonic raised-cosine) ← LFO(beat Hz)"
      contains: "createWaveShaper"
    - path: "src/audio/layers/monaural.ts"
      provides: "MonauralLayer: osc(f) + osc(f+beat), pre-gain 0.5 masing-masing → satu GainNode penjumlah"
      contains: "0.5"
    - path: "src/audio/engine.ts"
      provides: "Registry bertambah: isochronic, monaural"
      contains: "isochronic"
  key_links:
    - from: "src/audio/engine.ts"
      to: "src/audio/layers/isochronic.ts"
      via: "layer factory registry"
      pattern: "isochronic"
    - from: "src/audio/layers/isochronic.ts"
      to: "src/audio/constants.ts"
      via: "DEMO.isochronic (carrier solfeggio 528, beat 10)"
      pattern: "DEMO"
---

<objective>
Dua vertical slice entrainment berikutnya: isochronic tones (ENG-02) dengan envelope pulse halus via LFO → WaveShaper monotonic, dan monaural beats (ENG-03) via penjumlahan dua oscillator. Keduanya langsung muncul sebagai tombol audisi baru di UI (App me-render dari daftar kinds — tidak perlu edit App.tsx).

Purpose: melengkapi ketiga mode entrainment. Arsitektur LFO dipilih karena Phase 2 mewajibkan beat Hz di-ramp kontinu (ADR-006) — ramp beat = ramp satu AudioParam (lfo.frequency).
Output: tombol "Isochronic" dan "Monaural" berbunyi; test pulse-count dan penjumlahan lulus.
</objective>

## Phase Goal

**As a** pengguna, **I want to** menekan tombol play dan mendengar setiap jenis suara tersintesis (binaural, isochronic, monaural, solfeggio, ambient) dengan bersih, **so that** fondasi audio terbukti bekerja end-to-end.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-audio-engine-core/01-RESEARCH.md
@KNOWLEDGE.md
@src/audio/types.ts
@src/audio/constants.ts
@src/audio/engine.ts
@src/audio/ramps.ts
@src/audio/layers/binaural.ts

<interfaces>
<!-- Kontrak dari plan 01-01/01-02 — implement terhadap ini, jangan ubah signature. -->

```typescript
// src/audio/types.ts
export interface SoundLayer {
  readonly output: GainNode;
  start(t: number): void;
  stop(t: number): void;
}
export type LayerFactory = (ctx: BaseAudioContext) => SoundLayer;

// src/audio/engine.ts — pola registrasi (ikuti persis pola entri "binaural" yang sudah ada):
// LAYER_FACTORIES["isochronic"] = (ctx) => createIsochronicLayer(ctx, DEMO.isochronic);
// LAYER_FACTORIES["monaural"]   = (ctx) => createMonauralLayer(ctx, DEMO.monaural);
```

Dari src/audio/constants.ts: `DEMO.isochronic = { carrier: 528, beat: 10 }` (528 = solfeggio-as-carrier), `DEMO.monaural = { carrier: 200, beat: 10 }`, `FADE_SEC`.
Dari src/audio/ramps.ts: `fadeIn(param, target, t, dur?)`, `fadeOut(param, t, dur?)` — pakai untuk output.gain tiap layer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IsochronicLayer — LFO → WaveShaper monotonic → envGain.gain (test pulse-count dulu)</name>
  <files>src/audio/layers/isochronic.ts, src/audio/engine.ts, tests/audio/isochronic.test.ts</files>
  <read_first>
    - src/audio/layers/binaural.ts (pola layer yang sudah jalan: struktur factory, start/stop dengan fadeIn/fadeOut, penjadwalan osc.stop setelah fade)
    - src/audio/engine.ts (pola registrasi LAYER_FACTORIES — tambah entri, jangan ubah API)
    - .planning/phases/01-audio-engine-core/01-RESEARCH.md (Pattern 3 — kode terverifikasi empiris; Pitfall 1 — curve non-monotonic = double pulse, DIREPRODUKSI empiris: 20 pulse pada LFO 10 Hz)
    - tests/audio/binaural.test.ts (pola OfflineAudioContext + cast tipe)
  </read_first>
  <behavior>
    - Test 1 (pulse count = beat Hz, ENG-02): render OfflineAudioContext(1, 44100, 44100) IsochronicLayer beat 10 → deteksi onset pulse (envelope |sample| di-smooth window ~2ms; hitung crossing NAIK melewati threshold 0.1 dengan hysteresis turun 0.05) → jumlah onset TEPAT 10. Curve non-monotonic akan menghasilkan 20 → test ini adalah guard bug ganda (Pitfall 1)
    - Test 2 (silent gap nyata): di antara pulse, envelope turun < 0.01 (tone benar-benar off — ciri isochronic, bukan tremolo dangkal)
    - Test 3 (click-free, ENG-06): maxDelta sample-to-sample seluruh render < 0.15 (raised-cosine rise ~0.8/π rad memberi transisi halus; square LFO langsung akan menghasilkan step ≈ amplitudo penuh)
  </behavior>
  <action>
    Tulis tests/audio/isochronic.test.ts dulu (RED), lalu implement:
    1. src/audio/layers/isochronic.ts — `createIsochronicLayer(ctx: BaseAudioContext, params: ToneParams): SoundLayer`. Graph PERSIS Pattern 3 riset: carrierOsc(params.carrier) → envGain (gain.value = 0 — envelope 100% dari shaper) → output GainNode; lfo = OscillatorNode sine di params.beat Hz → WaveShaperNode → connect ke envGain.gain (audio-rate modulation). Curve WAJIB MONOTONIC non-decreasing, N = 2048: untuk tiap i, x = (i/(N-1))*2 - 1; p = clamp(x/0.8, 0, 1); curve[i] = 0.5*(1 - cos(PI*p)) — off saat x <= 0, naik halus 0→1 untuk x 0..0.8, plateau 1 setelahnya. JANGAN buat curve yang naik lalu turun (Pitfall 1). start(t): fadeIn output.gain + carrierOsc.start(t) + lfo.start(t); stop(t): fadeOut output.gain + kedua source .stop(t + FADE_SEC + 0.01).
    2. src/audio/engine.ts — tambah entri registry "isochronic" memakai DEMO.isochronic dari constants (carrier 528 = solfeggio-as-carrier; assertion resminya di plan 01-04). Jangan sentuh kode lain di engine.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit 0; tests/audio/isochronic.test.ts >= 3 assertion pass
    - Test pulse-count meng-assert nilai TEPAT 10 (bukan rentang yang meloloskan 20)
    - Loop pembuatan curve menghasilkan array non-decreasing: assertion test atau invariant `curve[i] >= curve[i-1]` untuk semua i
    - src/audio/layers/isochronic.ts tidak memuat setTimeout/setInterval/exponentialRampToValueAtTime (grep 0 match)
    - engine.implementedKinds kini memuat "isochronic" (assertion di test)
  </acceptance_criteria>
  <done>Tombol "Isochronic" muncul otomatis di UI dan memutar tone berdenyut 10 Hz halus; bug double-pulse mustahil lolos karena test exact-count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MonauralLayer — penjumlahan dua oscillator (test envelope beat + no-clip dulu)</name>
  <files>src/audio/layers/monaural.ts, src/audio/engine.ts, tests/audio/monaural.test.ts</files>
  <read_first>
    - src/audio/layers/binaural.ts (pola layer + start/stop)
    - .planning/phases/01-audio-engine-core/01-RESEARCH.md (Pattern 4 — unity summing; "Don't Hand-Roll" — penjumlahan via multiple connect ke satu GainNode)
    - src/audio/engine.ts (pola registrasi — state terkini setelah Task 1)
  </read_first>
  <behavior>
    - Test 1 (mono ke dua telinga, ENG-03): render OfflineAudioContext(2, 44100, 44100) via engine.play("monaural") → channel L dan R identik sample-per-sample (selisih maksimum < 1e-6) — beat fisik tersebar sama, bekerja di speaker
    - Test 2 (beat envelope 10 Hz): hitung RMS per window 5 ms sepanjang 1 detik → deret RMS memiliki ±10 minimum lokal dalam (toleransi 9-11) — bukti interferensi dua tone menghasilkan beat amplitudo di beat Hz
    - Test 3 (no clip): max |sample| <= 1.0 di seluruh render (pre-gain 0.5 + 0.5 menjamin jumlah <= 1.0)
  </behavior>
  <action>
    Tulis tests/audio/monaural.test.ts dulu (RED), lalu implement:
    1. src/audio/layers/monaural.ts — `createMonauralLayer(ctx: BaseAudioContext, params: ToneParams): SoundLayer`. Graph Pattern 4: oscA(params.carrier) → gainA(0.5); oscB(params.carrier + params.beat) → gainB(0.5); keduanya connect ke SATU GainNode penjumlah (Web Audio unity summing otomatis) → output GainNode. Pre-gain 0.5 dipasang saat konstruksi (sebelum audio berjalan — bukan pelanggaran ramp discipline), agar jumlah tidak clip. Output mono ter-upmix sama ke L/R oleh graph. start/stop: fadeIn/fadeOut output.gain + jadwal osc.stop setelah fade, pola sama dengan binaural.ts.
    2. src/audio/engine.ts — tambah entri registry "monaural" memakai DEMO.monaural.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit 0; tests/audio/monaural.test.ts >= 3 assertion pass
    - Assertion L === R (maks selisih < 1e-6) dan max |sample| <= 1.0 hadir eksplisit
    - Jumlah minima RMS-envelope di rentang 9-11 untuk beat 10 Hz
    - engine.implementedKinds memuat "binaural", "isochronic", "monaural" (urutan bebas)
    - Guard React-purity infra.test.ts tetap pass
  </acceptance_criteria>
  <done>Tombol "Monaural" muncul otomatis dan berbunyi; beat terbukti secara matematis lewat envelope RMS; tidak ada clipping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine → perangkat audio user | Keselamatan pendengaran (loudness) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service / Safety | Penjumlahan monaural (clipping > 0 dBFS = distorsi keras mendadak) | mitigate | Pre-gain 0.5 per oscillator; test no-clip max |sample| <= 1.0; master clamp dari plan 01-02 tetap berlaku |
| T-01-SC | Tampering | npm installs | accept | Tidak ada dependency baru di plan ini |
</threat_model>

<verification>
1. `npm test` → exit 0 (infra + ramps + binaural + isochronic + monaural)
2. `npm run build` → exit 0
3. `grep -rn "from ['\"]react" src/audio/` → 0 match
</verification>

<success_criteria>
- Ketiga mode entrainment (binaural, isochronic, monaural) terdaftar di engine dan berbunyi dari tombol audisi
- Pulse isochronic tepat beat Hz dengan gap senyap nyata dan tanpa click
- Monaural mono-identik L/R tanpa clipping
</success_criteria>

<output>
Create `.planning/phases/01-audio-engine-core/01-03-SUMMARY.md` when done
</output>
