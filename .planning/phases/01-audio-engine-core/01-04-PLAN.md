---
phase: 01-audio-engine-core
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - src/audio/layers/solfeggio.ts
  - src/audio/noise.ts
  - src/audio/layers/ambient.ts
  - src/audio/engine.ts
  - tests/audio/solfeggio.test.ts
  - tests/audio/ambient.test.ts
autonomous: false
requirements: [ENG-04, ENG-05]
must_haves:
  truths:
    - "User menekan tombol Solfeggio dan mendengar pure tone 528 Hz; solfeggio juga terpakai sebagai carrier (isochronic demo memakai 528)"
    - "User dapat mendengar keempat ambient tersintesis: hujan, ombak laut, angin, brown noise"
    - "Brown noise loop tidak menghasilkan 'tick' berkala di jahitan loop"
    - "Seluruh 8 suara terdengar bersih di telinga manusia dengan headphone — play/stop/volume tanpa click/pop"
  artifacts:
    - path: "src/audio/layers/solfeggio.ts"
      provides: "SolfeggioLayer: pure tone oscillator pada frekuensi solfeggio"
      contains: "createOscillator"
    - path: "src/audio/noise.ts"
      provides: "createWhiteNoiseBuffer + createBrownNoiseBuffer (leaky integrator 0.02/1.02, gain 3.5, crossfade jahitan loop)"
      exports: ["createWhiteNoiseBuffer", "createBrownNoiseBuffer"]
    - path: "src/audio/layers/ambient.ts"
      provides: "createAmbientLayer(ctx, kind) untuk rain|ocean|wind|brown — buffer loop + BiquadFilter + LFO"
      contains: "BiquadFilter"
    - path: "src/audio/engine.ts"
      provides: "Registry lengkap 8 kind: binaural, isochronic, monaural, solfeggio, rain, ocean, wind, brown"
      contains: "solfeggio"
  key_links:
    - from: "src/audio/layers/ambient.ts"
      to: "src/audio/noise.ts"
      via: "buffer generator dipanggil saat konstruksi layer"
      pattern: "create(White|Brown)NoiseBuffer"
    - from: "src/audio/layers/ambient.ts"
      to: "src/audio/constants.ts"
      via: "parameter filter/LFO dari DEMO.ambient (ASSUMED — tune by ear)"
      pattern: "DEMO"
    - from: "src/audio/engine.ts"
      to: "src/audio/layers/solfeggio.ts"
      via: "layer factory registry"
      pattern: "solfeggio"
---

<objective>
Vertical slice terakhir Phase 1: solfeggio sebagai pure-tone layer DAN sebagai carrier (ENG-04), plus keempat ambient tersintesis — rain, ocean, wind, brown noise (ENG-05). Fase ditutup dengan checkpoint verifikasi telinga manusia: test otomatis membuktikan sinyal benar secara matematis, tapi "terdengar bersih" butuh telinga (rekomendasi riset, Open Question 2).

Purpose: melengkapi seluruh palet suara Phase 1 sehingga goal fase tercapai penuh — setiap jenis suara terdengar bersih end-to-end.
Output: 8 tombol audisi berbunyi semua; test render ambient/solfeggio hijau; approval pendengaran manusia.
</objective>

## Phase Goal

**As a** pengguna, **I want to** menekan tombol play dan mendengar setiap jenis suara tersintesis (binaural, isochronic, monaural, solfeggio, ambient) dengan bersih, **so that** fondasi audio terbukti bekerja end-to-end.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-audio-engine-core/01-RESEARCH.md
@KNOWLEDGE.md
@src/audio/types.ts
@src/audio/constants.ts
@src/audio/engine.ts
@src/audio/ramps.ts
@src/audio/layers/binaural.ts

<interfaces>
<!-- Kontrak dari plan 01-01..01-03 — implement terhadap ini. -->

```typescript
// src/audio/types.ts
export type AmbientKind = "rain" | "ocean" | "wind" | "brown";
export interface SoundLayer {
  readonly output: GainNode;
  start(t: number): void;
  stop(t: number): void;
}

// Pola registrasi engine (ikuti entri yang sudah ada):
// LAYER_FACTORIES["solfeggio"] = (ctx) => createSolfeggioLayer(ctx, DEMO.solfeggio.tone);
// for kind of ["rain","ocean","wind","brown"]: LAYER_FACTORIES[kind] = (ctx) => createAmbientLayer(ctx, kind);
```

Dari src/audio/constants.ts: `SOLFEGGIO.healing = 528`, `DEMO.solfeggio.tone = 528`, `DEMO.isochronic.carrier = 528` (solfeggio-as-carrier), `DEMO.ambient.{ocean,wind,rain}` (nilai numerik ASSUMED — boleh disesuaikan saat tuning, tetap terpusat di constants.ts), `FADE_SEC`.
LFO→gain trick dari riset (Pattern 5, ConstantSourceNode terverifikasi di node-web-audio-api): `gain.gain.value = baseLevel` saat konstruksi; LFO → GainNode kecil (depth) → connect ke `gain.gain` — AudioParam menjumlahkan intrinsic value + sinyal input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SolfeggioLayer (pure tone) + assertion solfeggio-as-carrier</name>
  <files>src/audio/layers/solfeggio.ts, src/audio/engine.ts, tests/audio/solfeggio.test.ts</files>
  <read_first>
    - src/audio/layers/binaural.ts (pola layer paling sederhana untuk direplikasi: osc → gain → output, start/stop dengan ramps)
    - src/audio/constants.ts (SOLFEGGIO, DEMO.solfeggio, DEMO.isochronic)
    - src/audio/engine.ts (state registry terkini setelah plan 01-03)
    - KNOWLEDGE.md (daftar frekuensi solfeggio: 174..963; 528 = healing)
  </read_first>
  <behavior>
    - Test 1 (pure tone, ENG-04): render OfflineAudioContext(1, 44100, 44100) SolfeggioLayer(528) → zero-crossings ≈ 528 (±6)
    - Test 2 (carrier mode, ENG-04): assert DEMO.isochronic.carrier === SOLFEGGIO.healing — bukti terprogram bahwa solfeggio dipakai sebagai carrier entrainment (isochronic demo membawa 528 Hz)
    - Test 3 (registry): engine.implementedKinds memuat "solfeggio"
  </behavior>
  <action>
    Tulis tests/audio/solfeggio.test.ts dulu (RED), lalu implement:
    1. src/audio/layers/solfeggio.ts — `createSolfeggioLayer(ctx: BaseAudioContext, toneHz: number): SoundLayer`: satu OscillatorNode sine pada toneHz → output GainNode. start(t): fadeIn output.gain + osc.start(t); stop(t): fadeOut + osc.stop(t + FADE_SEC + 0.01). Ini layer paling tipis — replikasi pola binaural.ts tanpa merger.
    2. src/audio/engine.ts — tambah entri registry "solfeggio" memakai DEMO.solfeggio.tone.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit 0; tests/audio/solfeggio.test.ts >= 3 assertion pass
    - Zero-crossing terukur 522-534 pada render solfeggio
    - Assertion `DEMO.isochronic.carrier === SOLFEGGIO.healing` ada dan pass
    - src/audio/layers/solfeggio.ts zero React import (guard infra tetap pass)
  </acceptance_criteria>
  <done>Tombol "Solfeggio 528 Hz" berbunyi pure tone; peran solfeggio sebagai carrier terkunci oleh assertion konstanta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Noise generators + AmbientLayer (rain, ocean, wind, brown) + registrasi</name>
  <files>src/audio/noise.ts, src/audio/layers/ambient.ts, src/audio/engine.ts, tests/audio/ambient.test.ts</files>
  <read_first>
    - .planning/phases/01-audio-engine-core/01-RESEARCH.md (Pattern 5 — resep noise + tabel per-ambient + LFO→gain trick; Pitfall 4 — loop seam brown noise; Anti-pattern ScriptProcessorNode)
    - src/audio/constants.ts (DEMO.ambient — SEMUA parameter numerik ambil dari sini, jangan hardcode di layer)
    - src/audio/layers/isochronic.ts (pola layer dengan LFO yang sudah jalan)
    - src/audio/engine.ts (state registry terkini)
  </read_first>
  <behavior>
    - Test 1 (semua kind berbunyi, ENG-05): untuk tiap kind rain|ocean|wind|brown, render 2 detik → RMS keseluruhan > 0.01 (non-silent) DAN max |sample| <= 1.0 (no clip)
    - Test 2 (loop seam brown, Pitfall 4): buffer dari createBrownNoiseBuffer → |data[length-1] - data[0]| < 0.05 (crossfade jahitan bekerja; tanpa fix, random walk berakhir jauh dari titik awal)
    - Test 3 (identitas spektral kasar): zero-crossing rate render "rain" (highpass 1000 Hz) > 3x zero-crossing rate render "ocean" (lowpass 400 Hz) — bukti murah bahwa filter benar terpasang dan kedua ambient berbeda karakter
    - Test 4 (registry lengkap): engine.implementedKinds memuat kedelapan kind: binaural, isochronic, monaural, solfeggio, rain, ocean, wind, brown
  </behavior>
  <action>
    Tulis tests/audio/ambient.test.ts dulu (RED), lalu implement:
    1. src/audio/noise.ts — JANGAN ScriptProcessorNode (deprecated). `createWhiteNoiseBuffer(ctx, seconds = 5)`: AudioBuffer 1 channel, tiap sample Math.random()*2-1. `createBrownNoiseBuffer(ctx, seconds = 10)`: leaky integrator per riset — white = Math.random()*2-1; d[i] = (last + 0.02*white)/1.02; last = d[i]; d[i] *= 3.5 — lalu FIX JAHITAN LOOP: crossfade linear ~50 ms terakhir buffer menuju nilai d[0] (mis. untuk n sample terakhir, d[i] = lerp(d[i], d[0], progress)) agar loop tidak "tick" berkala.
    2. src/audio/layers/ambient.ts — `createAmbientLayer(ctx: BaseAudioContext, kind: AmbientKind): SoundLayer`. Semua source = AudioBufferSourceNode dengan loop = true. Graph per kind sesuai tabel Pattern 5, parameter dari DEMO.ambient:
       - brown: brown buffer loop → output (statis, tanpa filter)
       - ocean: brown buffer loop → BiquadFilter lowpass (DEMO.ambient.ocean.lowpassHz) → GainNode dengan gain.value = baseGain, dimodulasi LFO sine (lfoHz) → GainNode depth (lfoDepth) → connect ke gain.gain (LFO→gain trick) → output
       - wind: white buffer loop → BiquadFilter bandpass (bandpassHz, Q = q) dengan LFO (lfoHz) → GainNode depth (sweepHz) → connect ke filter.frequency → output
       - rain: white buffer loop → BiquadFilter highpass (highpassHz) → BiquadFilter lowpass (lowpassHz) → output (statis)
       start(t): fadeIn output.gain + semua source/LFO .start(t); stop(t): fadeOut + .stop(t + FADE_SEC + 0.01). Komentari parameter estetik dengan `// ASSUMED — tune by ear` sesuai riset; bila perlu penyesuaian nilai, ubah HANYA di constants.ts.
    3. src/audio/engine.ts — daftarkan keempat kind ambient via satu loop/empat entri memakai createAmbientLayer(ctx, kind).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit 0; tests/audio/ambient.test.ts >= 4 test pass (mencakup keempat kind)
    - Assertion seam brown |d[N-1] - d[0]| < 0.05 pass
    - Assertion ZCR(rain) > 3 * ZCR(ocean) pass
    - `grep -rn "ScriptProcessor" src/` → 0 match
    - Semua angka parameter ambient di src/audio/layers/ambient.ts berasal dari import constants (grep angka literal 400/600/1000/7000 di ambient.ts = 0 match)
    - engine.implementedKinds berisi tepat 8 kind
  </acceptance_criteria>
  <done>Keempat tombol ambient berbunyi dengan karakter berbeda (hujan cerah/berdesis, ombak berayun pelan, angin menyapu, brown dalam); loop bebas tick; palet 8 suara lengkap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifikasi telinga manusia — dengarkan semua suara dengan headphone</name>
  <read_first>
    - (untuk executor yang menyiapkan checkpoint) src/audio/constants.ts — bila user melaporkan ambient kurang enak, parameter di-tune di file ini saja
  </read_first>
  <what-built>Audio engine lengkap: 3 mode entrainment (binaural, isochronic, monaural), solfeggio pure tone, 4 ambient tersintesis — semuanya lulus unit test render nyata (frekuensi, pulse count, envelope, no-clip, no-click, loop seam). Yang belum terverifikasi: persepsi telinga manusia — khususnya efek stereo binaural (butuh dua telinga) dan kualitas estetik ambient (parameter ASSUMED).</what-built>
  <how-to-verify>
    1. Jalankan `npm run dev` lalu buka URL localhost yang tercetak di terminal — PAKAI HEADPHONE
    2. Klik "Binaural": pastikan tone terdengar SEDIKIT BERBEDA antara telinga kiri dan kanan, dan muncul sensasi "denyut" pelan ~10x/detik di kepala; lepas satu sisi headphone — sensasi denyut hilang (bukti beda L/R)
    3. Klik "Isochronic": tone berdenyut jelas on-off ~10x/detik, TANPA click/tick tajam di tiap pulse
    4. Klik "Monaural": denyut terdengar sama di kedua telinga (coba juga lewat speaker laptop)
    5. Klik "Solfeggio 528 Hz": pure tone stabil tunggal
    6. Klik masing-masing "Hujan", "Ombak Laut", "Angin", "Brown Noise": keempatnya berbunyi dengan karakter berbeda; dengarkan Brown/Ombak >= 15 detik — pastikan TIDAK ada "tick" berkala (bug jahitan loop)
    7. Selama suara berjalan: geser slider volume naik-turun (harus halus, tanpa click), klik Stop (fade-out halus), klik jenis suara lain saat suara masih berjalan (transisi tanpa pop)
    8. Bila ada ambient yang terdengar kurang natural: sebutkan yang mana — parameternya ASSUMED dan akan di-tune di src/audio/constants.ts
  </how-to-verify>
  <action>Pastikan `npm test` dan `npm run build` hijau, jalankan `npm run dev`, sajikan langkah verifikasi bernomor di atas kepada user, lalu BERHENTI dan tunggu hasil pendengaran. Bila user melaporkan masalah karakter ambient, tune nilai di src/audio/constants.ts (satu-satunya tempat parameter), jalankan ulang test, dan minta user mendengarkan lagi sebelum approve.</action>
  <verify>
    <human-check>User mendengarkan kedelapan suara dengan headphone mengikuti langkah 1-8 dan melaporkan hasil</human-check>
  </verify>
  <acceptance_criteria>
    - User mengkonfirmasi kedelapan suara terdengar dan bersih (tanpa click/pop) pada play, stop, ganti suara, dan perubahan volume
    - User mengkonfirmasi efek binaural terasa beda antar telinga
    - Keluhan estetik ambient (jika ada) dicatat di SUMMARY sebagai catatan tuning, dengan perbaikan parameter di constants.ts bila user memintanya sebelum approve
  </acceptance_criteria>
  <done>User mengetik "approved" — goal Phase 1 terverifikasi oleh telinga manusia; catatan tuning (bila ada) masuk SUMMARY.</done>
  <resume-signal>Ketik "approved" bila semua bersih, atau jelaskan suara mana yang bermasalah (click/tick/karakter kurang pas)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine → perangkat audio user | Keselamatan pendengaran (loudness), terutama noise broadband via headphone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service / Safety | Ambient noise (broadband bisa terasa lebih keras dari tone pada gain sama) | mitigate | Test no-clip max sample <= 1.0 per kind; fade-in wajib dari 0; master clamp <= 1.0 (plan 01-02) tetap memayungi; checkpoint Task 3 memverifikasi kenyamanan volume by ear |
| T-01-07 | Tampering (input) | Parameter layer (freq/gain) | mitigate | Semua parameter berasal dari konstanta internal constants.ts (bukan input eksternal); clamp gain 0..1 di engine — defensive, murah (ASVS V5 minimal sesuai riset) |
| T-01-SC | Tampering | npm installs | accept | Tidak ada dependency baru di plan ini |
</threat_model>

<verification>
1. `npm test` → exit 0 (seluruh suite: infra, ramps, binaural, isochronic, monaural, solfeggio, ambient)
2. `npm run build` → exit 0
3. `grep -rn "from ['\"]react" src/audio/` → 0 match (ENG-07 utuh sampai akhir fase)
4. Checkpoint pendengaran manusia approved
</verification>

<success_criteria>
- Kedelapan jenis suara terdaftar, teruji render, dan terdengar bersih oleh telinga manusia dengan headphone
- Brown/ocean loop bebas tick berkala; rain vs ocean terbukti beda spektral
- Solfeggio bekerja sebagai pure tone DAN sebagai carrier (isochronic 528)
- Goal Phase 1 tercapai: fondasi audio bekerja end-to-end
</success_criteria>

<output>
Create `.planning/phases/01-audio-engine-core/01-04-SUMMARY.md` when done
</output>
