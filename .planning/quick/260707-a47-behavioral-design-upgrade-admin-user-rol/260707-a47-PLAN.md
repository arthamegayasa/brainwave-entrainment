---
phase: quick-260707-a47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0002_roles_library.sql
  - src/lib/roles.ts
  - src/lib/audioLibrary.ts
  - src/lib/useEntitlement.ts
  - src/state/customPresets.ts
  - src/state/tier.ts
  - src/state/progress.ts
  - src/audio/presets.ts
  - src/ui/builderEngine.ts
  - src/ui/Library.tsx
  - src/App.tsx
  - src/ui/Home.tsx
  - src/ui/Builder.tsx
  - src/ui/Upgrade.tsx
  - src/ui/Player.tsx
  - src/ui/Landing.tsx
  - src/App.css
  - tests/state.test.ts
  - tests/progress.test.ts
autonomous: true
requirements: [QUICK-A47]
user_setup:
  - service: supabase
    why: "Roles/library schema must be applied to the project database; first admin must be promoted manually"
    dashboard_config:
      - task: "Run migration 0002_roles_library.sql (supabase db push or SQL editor)"
        location: "Supabase Dashboard -> SQL Editor (or supabase CLI)"
      - task: "Promote your account: update public.profiles set role='admin' where email='<your-email>'"
        location: "Supabase Dashboard -> SQL Editor"
must_haves:
  truths:
    - "All 8 preset cards show goal-first names (Sleeping, Meditating, Relaxing, Calming Anxiety, Focus & Concentration, Boosting Energy, Creative Flow, Power Nap); preset ids unchanged"
    - "Normal user sees a Library nav item and no Studio; admin sees Studio; opening studio view as non-admin renders Library"
    - "Signed-in user sees cloud audios assigned to them under 'Made for you' plus templates; local saved sessions listed with JSON import/export; all playable"
    - "Admin with Supabase configured can publish the current Builder design as a template or assign it to a user picked from an email list"
    - "Home shows a time-of-day 'Recommended now' one-tap card above the grid and a 4-step journey strip that never shows 0%"
    - "First visit after Landing shows a skippable multi-select goal picker; chosen goals reorder the grid first"
    - "After the 2nd completed session a dismissible reciprocity card appears on Home linking to Upgrade; never before 2 sessions; playback never requires an account"
    - "Upgrade annual hero shows struck-through Rp588,000 anchor beside Rp249,000, keeps Save 58% badge and Rp20,750/mo line, and shows a personal-stakes streak block when progress exists"
    - "Session-complete view shows 'Session #N this week', Mon-Sun streak dots, and 'Come back tomorrow to keep your streak.'"
    - "npm run build and npm test pass; src/audio/** has no React imports; ALL_UNLOCKED/tier gating unchanged"
  artifacts:
    - path: "supabase/migrations/0002_roles_library.sql"
      provides: "profiles + is_admin() + custom_audios + audio_assignments with RLS"
      contains: "create table if not exists public.profiles"
    - path: "src/lib/roles.ts"
      provides: "fetchProfile() role lookup"
      exports: ["fetchProfile"]
    - path: "src/lib/audioLibrary.ts"
      provides: "cloud audio CRUD (user + admin)"
      exports: ["listAssignedAudios", "listAllUsers", "publishAudio", "assignAudio", "unassignAudio", "listMyPublishedAudios", "deleteAudio"]
    - path: "src/state/progress.ts"
      provides: "journey, streaks, recommendations, reciprocity state (serenade.progress.v1)"
      exports: ["loadProgress", "recordSessionCompleted", "recommendedPresetId"]
    - path: "src/ui/Library.tsx"
      provides: "Made for you + My saved sessions views with playback"
      min_lines: 80
    - path: "tests/progress.test.ts"
      provides: "unit tests for journey/streak/recommendation logic"
      min_lines: 40
  key_links:
    - from: "src/lib/audioLibrary.ts"
      to: "src/state/customPresets.ts"
      via: "sanitizeSession re-sanitizes untrusted cloud spec jsonb on read"
      pattern: "sanitizeSession"
    - from: "src/App.tsx"
      to: "src/lib/useEntitlement.ts"
      via: "role drives NAV items + studio fallback"
      pattern: "role"
    - from: "src/ui/Home.tsx"
      to: "src/state/progress.ts"
      via: "recommendedPresetId + journey + reciprocity"
      pattern: "recommendedPresetId"
    - from: "src/App.tsx"
      to: "src/state/progress.ts"
      via: "recordSessionCompleted on session end / >=5 min listened"
      pattern: "recordSessionCompleted"
    - from: "src/ui/Upgrade.tsx"
      to: "src/state/tier.ts"
      via: "anchorAnnual contrast price"
      pattern: "anchorAnnual"
    - from: "src/ui/Library.tsx"
      to: "src/ui/builderEngine.ts"
      via: "shared BuilderEngine singleton (same custom-session start path as Builder)"
      pattern: "ensureBuilder"
---

<objective>
Behavioral-design upgrade + two-role system for Serenade: goal-first preset renames, Supabase roles schema (admin creates/assigns custom audios; users get a Library of templates + assigned + saved sessions), and six behavioral-economics features (smart defaults, goal gradient, IKEA-effect personalization, reciprocity, loss aversion, contrast anchoring) — all client state in localStorage, no new npm dependencies.

Purpose: convert the feature-complete PWA into a habit-forming, monetization-ready product with an admin channel for personalized audio delivery.
Output: migration 0002, role-aware client lib, progress state module with tests, and rewired UI (Home/Library/Builder/Upgrade/Player/Landing/App).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@src/App.tsx
@src/ui/Home.tsx
@src/ui/Builder.tsx
@src/ui/Upgrade.tsx
@src/ui/Player.tsx
@src/ui/Landing.tsx
@src/ui/useSession.ts
@src/audio/presets.ts
@src/audio/builder.ts
@src/state/prefs.ts
@src/state/tier.ts
@src/state/customPresets.ts
@src/lib/supabase.ts
@src/lib/payments.ts
@src/lib/useEntitlement.ts
@supabase/migrations/0001_payments.sql
@tests/state.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/audio/builder.ts:
```typescript
export interface BuilderLayerSpec { id: string; type: BuilderLayerType; carrierHz: number; beatMode: "follow" | "fixed"; fixedBeatHz: number; gain: number; }
export interface BuilderCurve { startHz: number; targetHz: number; endHz: number | null; rampInMin: number; rampOutMin: number; }
export interface CustomSession { version: 1; id: string; name: string; curve: BuilderCurve; layers: BuilderLayerSpec[]; createdAt: string; }
export class BuilderEngine { constructor(ctx: BaseAudioContext); start(layers, curve, durationMin): void; stop(): void; progress(): { elapsedSec; remainingSec }; get isRunning(): boolean; }
```

From src/state/customPresets.ts (private today — Task 1 exports it):
```typescript
function sanitizeSession(value: unknown): CustomSession | null  // clamps Hz/gain, validates layers
export function listCustomSessions(): CustomSession[]
export function saveCustomSession(session: CustomSession): void
export function deleteCustomSession(id: string): void
export function exportSessionJSON(session: CustomSession): string
export function importSessionJSON(json: string): CustomSession
```

From src/lib/supabase.ts:
```typescript
export const isPaymentsConfigured: boolean;
export const supabase: SupabaseClient | null;
```

From src/lib/useEntitlement.ts:
```typescript
export interface AuthEntitlementState { configured: boolean; loading: boolean; email: string | null; entitlement: Entitlement | null; isPremium: boolean; refresh: () => Promise<void>; }
```

From src/state/prefs.ts:
```typescript
export interface Prefs { volumes; lastPresetId: string | null; lastDurationMin: number | null | "inf"; lastMode: ListeningMode; lastAmbient: AmbientKind | null; visited: boolean; }
export function loadPrefs(): Prefs; export function savePrefs(patch: Partial<Prefs>): void;
```

From src/ui/useSession.ts (App already owns this):
```typescript
useSession(onEnded?: () => void): SessionApi  // onEnded fires when engine stops naturally; state.progress.elapsedSec available while active
```

From tests/state.test.ts — localStorage shim pattern to reuse in tests/progress.test.ts:
```typescript
function installLocalStorage() { const store = new Map<string, string>(); (globalThis as Record<string, unknown>).localStorage = { getItem, setItem, removeItem, clear, key, length }; }
```
</interfaces>
</context>

## Source Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| CONTEXT | D-01 preset renames (ids unchanged) | Task 1 |
| CONTEXT | D-02 roles schema migration 0002 | Task 1 |
| CONTEXT | D-03 client role layer (roles.ts, audioLibrary.ts, useEntitlement role, sanitize on read, localStorage override) | Task 1 |
| CONTEXT | D-04 role-gated UI (admin Studio, user Library, publish panel) | Task 3 |
| CONTEXT | D-05 behavioral-economics features (smart defaults, goal gradient, IKEA, reciprocity, loss aversion, anchoring) | Task 2 (state) + Task 3 (UI); anchorAnnual constant in Task 1 |
| CONTEXT | D-06 constraints (no React in audio engine, presets centralized, no new deps, build+test pass, English copy, tier gating unchanged, payments untouched) | All tasks (verified in Task 3 gate) |
| GOAL | Behavioral-design upgrade + two-role system | Tasks 1-3 |

All items COVERED. Deferred/excluded: none.

<tasks>

<task type="auto">
  <name>Task 1: Data layer — roles migration, cloud library API, role-aware entitlement, pricing anchor, preset renames</name>
  <files>supabase/migrations/0002_roles_library.sql, src/lib/roles.ts, src/lib/audioLibrary.ts, src/lib/useEntitlement.ts, src/state/customPresets.ts, src/state/tier.ts, src/audio/presets.ts, src/ui/Landing.tsx, tests/state.test.ts</files>
  <action>
Implement D-01, D-02, D-03 and the D-05 anchoring constant.

1. **Preset renames (D-01)** in src/audio/presets.ts — change ONLY `name` (and taglines where they no longer read naturally with the new name); ids, Hz values, carriers, ambient defaults, premium flags all unchanged: deep-sleep -> "Sleeping", deep-meditation -> "Meditating", healing-relaxation -> "Relaxing", anxiety-relief -> "Calming Anxiety", focus -> "Focus & Concentration", energy -> "Boosting Energy", creativity -> "Creative Flow", power-nap -> "Power Nap" (unchanged). Update src/ui/Landing.tsx copy that references old names ("From Deep Sleep to Power Nap" in FEATURES -> "From Sleeping to Power Nap"; align the STEPS goal phrasing with the new names). All copy stays English.

2. **Migration (D-02)** — new file supabase/migrations/0002_roles_library.sql, following the style of 0001_payments.sql:
   - `public.profiles`: user_id uuid primary key references auth.users(id) on delete cascade, email text, role text not null default 'user' check (role in ('user','admin')), display_name text, created_at timestamptz not null default now().
   - Function `public.handle_new_user()` returns trigger, `security definer`, `set search_path = public`, language plpgsql: inserts (new.id, new.email) into profiles on conflict (user_id) do nothing; trigger `on_auth_user_created` after insert on auth.users for each row. Backfill statement: insert into profiles (user_id, email) select id, email from auth.users on conflict do nothing.
   - Function `public.is_admin()` returns boolean, `security definer`, `stable`, `set search_path = public`: returns exists(select 1 from public.profiles where user_id = auth.uid() and role = 'admin'). Comment WHY security definer: prevents RLS recursion when profiles policies call it.
   - `public.custom_audios`: id uuid primary key default gen_random_uuid(), created_by uuid references auth.users(id), name text not null, goal_tagline text, spec jsonb not null, is_template boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz not null default now().
   - `public.audio_assignments`: audio_id uuid references custom_audios(id) on delete cascade, user_id uuid references auth.users(id) on delete cascade, assigned_by uuid references auth.users(id), created_at timestamptz not null default now(), primary key (audio_id, user_id).
   - RLS on all three tables: profiles — select policy using (auth.uid() = user_id or public.is_admin()); NO insert/update/delete policies (role changes via SQL/service role only). custom_audios — one policy "admin full access" for all using/with check public.is_admin(); one select policy to authenticated using (is_template = true or exists(select 1 from public.audio_assignments a where a.audio_id = id and a.user_id = auth.uid())). audio_assignments — "admin manage" for all using/with check public.is_admin(); select policy using (auth.uid() = user_id).

3. **Export sanitizer (D-03)**: in src/state/customPresets.ts change the private `sanitizeSession` to `export function sanitizeSession` (no behavior change). Cloud specs are untrusted data and MUST pass through it on read.

4. **src/lib/roles.ts (D-03)**: export `type Role = "user" | "admin"` and `fetchProfile(): Promise<{ role: Role; email: string | null } | null>` — returns null when supabase is null or signed out; selects role,email from profiles where user_id = auth.uid() via maybeSingle; any unexpected role value coerces to "user".

5. **src/lib/audioLibrary.ts (D-03)**: export `interface CloudAudio { id: string; name: string; goalTagline: string | null; spec: CustomSession; isTemplate: boolean; createdAt: string; }`. Functions (all throw Error("Library not configured") when supabase is null):
   - `listAssignedAudios(): Promise<CloudAudio[]>` — select * from custom_audios (RLS returns templates + rows assigned to the caller); map each row's `spec` through `sanitizeSession` from customPresets.ts and DROP rows whose spec fails sanitization (untrusted data — never feed raw jsonb to the audio engine).
   - Admin functions: `listAllUsers(): Promise<Array<{ userId: string; email: string | null }>>` (select user_id,email from profiles, order by email); `publishAudio(spec: CustomSession, opts: { name: string; goalTagline?: string; isTemplate: boolean }): Promise<string>` (insert into custom_audios with created_by = current auth uid, returns new id); `assignAudio(audioId: string, userId: string): Promise<void>` (upsert into audio_assignments with assigned_by = current uid); `unassignAudio(audioId: string, userId: string): Promise<void>`; `listMyPublishedAudios(): Promise<CloudAudio[]>` (created_by = current uid, sanitized the same way); `deleteAudio(audioId: string): Promise<void>`.

6. **useEntitlement role (D-03)**: extend AuthEntitlementState with `role: "user" | "admin"` (default "user"). When configured: fetch via `fetchProfile()` inside `refresh` alongside entitlement. When NOT configured: read localStorage key `serenade.role.override` — value "admin" yields role "admin" — with a code comment documenting this is a local/demo override for enabling Studio without a backend; the override is IGNORED when Supabase is configured (server profile wins).

7. **tier.ts anchor (D-05 anchoring)**: add `anchorAnnual: { amount: 588000, price: "Rp588,000" }` inside PRICING.IDR with a comment stating it is derived as 12 x monthly (49,000 x 12) and exists purely as a contrast anchor — do NOT change monthly/annual amounts, ALL_UNLOCKED, or any gating logic.

8. **tests/state.test.ts**: add assertions that PRICING.IDR.anchorAnnual.amount === PRICING.IDR.monthly.amount * 12 and that getPreset("deep-sleep").name === "Sleeping" (import getPreset from src/audio/presets).

Do NOT touch supabase/functions/** or 0001_payments.sql (D-06).
  </action>
  <verify>
    <automated>npm run build && npm test && node -e "const fs=require('fs');const sql=fs.readFileSync('supabase/migrations/0002_roles_library.sql','utf8');['public.profiles','public.is_admin','public.custom_audios','public.audio_assignments','enable row level security','security definer'].forEach(t=>{if(!sql.toLowerCase().includes(t.toLowerCase()))throw new Error('missing: '+t)});console.log('migration ok')"</automated>
  </verify>
  <done>Build + tests green; migration contains all four objects with RLS; sanitizeSession exported; audioLibrary/roles compile with the exact exported names above; useEntitlement returns role with documented localStorage fallback; anchorAnnual present without changing existing amounts; 8 presets renamed with ids untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavioral progress state (serenade.progress.v1) + unit tests</name>
  <files>src/state/progress.ts, tests/progress.test.ts</files>
  <behavior>
    - loadProgress() on empty storage auto-initializes discoveredAt (journey step 1 complete) and persists — journeyPercent() is always >= 25, never 0
    - recommendedPresetId(new Date("2026-07-07T08:00")) === "focus"; 12:00 -> "power-nap"; 16:00 -> "energy"; 19:00 -> "healing-relaxation"; 22:00 and 03:00 -> "deep-sleep" (boundaries: 05:00 starts focus, 10:59 still focus, 11:00 power-nap, 21:00 deep-sleep, 04:59 deep-sleep)
    - setChosenGoals(["focus","deep-sleep"]) completes journey step 2 and persists order; skipGoalPicker() marks the picker seen WITHOUT completing step 2
    - recordSessionCompleted("focus") x1 completes step 3; x3 completes step 4; journeyPercent() === 100 after all four
    - sessionsThisWeek(now) counts only completions in the current Mon-Sun week; weeklyStreakDots(now) returns boolean[7] indexed Mon..Sun with true on days having >= 1 completion
    - currentStreakDays(now): consecutive calendar days with >= 1 completion ending today (yesterday-ending streak still counts, so a morning visit before today's session shows the live streak)
    - shouldShowReciprocity() is false at 0 and 1 completed sessions, true at 2+, false again after dismissReciprocityCard(); persists across loadProgress() calls
    - corrupt localStorage JSON never throws — falls back to fresh defaults (same resilience pattern as prefs.ts)
  </behavior>
  <action>
Implement D-05's state layer as a pure-TS module (NO React imports) storing under localStorage key `serenade.progress.v1`, mirroring the storage()/try-catch resilience pattern of src/state/prefs.ts.

State shape (version: 1): discoveredAt (ISO string | null), chosenGoals (string[] of preset ids), goalPickerDone (boolean — true after choose OR skip), completedSessions (array of { at: ISO string, ref: string } — cap stored length at 400, drop oldest), reciprocityDismissed (boolean).

Exports: `loadProgress()`, `setChosenGoals(ids: string[])`, `skipGoalPicker()`, `recordSessionCompleted(ref: string)`, `journeySteps(): Array<{ label: string; done: boolean }>` with the four D-05 labels ("Discovered Serenade", "Personalized your goals", "First session completed", "3 sessions completed"), `journeyPercent(): number`, `recommendedPresetId(now?: Date): string` (time windows per D-05: 05:00-10:59 "focus", 11:00-14:59 "power-nap", 15:00-17:59 "energy", 18:00-20:59 "healing-relaxation", 21:00-04:59 "deep-sleep" — return preset IDS, the UI resolves names via getPreset), `sessionsThisWeek(now?: Date): number`, `weeklyStreakDots(now?: Date): boolean[]` (length 7, Monday-first), `currentStreakDays(now?: Date): number`, `shouldShowReciprocity(): boolean` (completedSessions.length >= 2 && !reciprocityDismissed), `dismissReciprocityCard()`, `totalSessions(): number`.

All date-dependent functions accept an optional `now: Date` defaulting to new Date() so tests are deterministic. Week starts Monday (getDay() 1); handle Sunday (getDay() 0) as index 6. Compare calendar days in local time.

Write tests FIRST in tests/progress.test.ts (vitest, reuse the installLocalStorage() shim pattern from tests/state.test.ts, beforeEach fresh storage) covering every case in the behavior block, then implement until green.
  </action>
  <verify>
    <automated>npx vitest run tests/progress.test.ts && node -e "const s=require('fs').readFileSync('src/state/progress.ts','utf8');if(/from ['\"]react/.test(s))throw new Error('React import in state module')" && npm run build</automated>
  </verify>
  <done>tests/progress.test.ts passes with all behavior cases; progress.ts has no React imports; full build still green.</done>
</task>

<task type="auto">
  <name>Task 3: UI wiring — role-gated nav, Library, publish panel, behavioral Home/Upgrade/session-complete</name>
  <files>src/App.tsx, src/ui/Home.tsx, src/ui/Library.tsx, src/ui/builderEngine.ts, src/ui/Builder.tsx, src/ui/Upgrade.tsx, src/ui/Player.tsx, src/App.css</files>
  <action>
Implement D-04 and D-05 UI on top of Task 1+2 modules. All copy English; reuse existing App.css design tokens (dark calm theme, chips, sheet, pill-btn, start-btn) and add new classes at the end of App.css.

1. **Shared engine (src/ui/builderEngine.ts)**: extract Builder.tsx's module-level `ctx`/`engine` singletons and `ensureBuilder()` into this new module; export `ensureBuilder(): Promise<BuilderEngine>` and `getBuilderEngine(): BuilderEngine | null`. Builder.tsx imports from it (delete its local copies). Library uses the SAME module so both share one AudioContext — this satisfies D-04's "same custom-session start path Builder uses".

2. **App.tsx (D-04 + D-05 goal gradient)**: extend View union with "library"; call `useEntitlement()` once at App level and derive `role`. NAV becomes role-dependent: [Sessions, Library, Science, Premium] for everyone, with Studio inserted after Library ONLY when role === "admin". When view === "studio" && role !== "admin", render Library (fallback per D-04). Call `loadProgress()` once on mount (auto-completes journey step 1). Session-completion recording (D-05 goal gradient): keep a ref holding the active preset id set in handleStart; in the useSession onEnded callback (natural end) call `recordSessionCompleted(ref)` and set a `completed: { presetName: string } | null` state; in handleExit (manual stop) do the same ONLY when `session.state.progress.elapsedSec >= 300` (>= 5 minutes listened), else go straight home. When `completed` is set, render the SessionComplete view (from Player.tsx, below) instead of Home until dismissed. Pass `onUpgrade={() => setView("upgrade")}` to Home and Library.

3. **SessionComplete (in src/ui/Player.tsx, new export)**: props { presetName, onDone }. Shows a calm completion card: preset name, "Session #{sessionsThisWeek()} this week", seven Mon-Sun streak dots (filled = weeklyStreakDots() true; small labels M T W T F S S), the loss-aversion line "Come back tomorrow to keep your streak." (D-05), and a "Back to sessions" button calling onDone. Live Player component itself is unchanged.

4. **Home.tsx (D-05 smart defaults + goal gradient + IKEA + reciprocity)**: add props `onUpgrade: () => void`.
   - "Recommended now" hero card ABOVE the grid: resolve `getPreset(recommendedPresetId())`; show emoji, name, tagline, a "Start now" button that calls onStart immediately with { preset, durationMin: prefs.lastDurationMin === "inf" ? null : prefs.lastDurationMin ?? 30, mode: prefs.lastMode, ambient: preset.defaultAmbient, solfeggioTone: null } (one tap, no sheet), and a secondary "Adjust" button that opens the existing SetupSheet for that preset. Grid cards remain the adjust path — SetupSheet unchanged.
   - Journey strip below the hero: compact progress bar (journeyPercent()) + the four step labels from journeySteps() with done/pending styling; the "Personalized your goals" step is a button that re-opens the goal picker (recovers from skip).
   - Goal picker overlay (IKEA effect): render when `!loadProgress().goalPickerDone` (first visit after Landing) — reuse sheet-backdrop/sheet styling; heading "What do you want more of?"; multi-select chips of the 8 goals (emoji + name from PRESETS); "Continue" calls setChosenGoals(selection) (completes step 2), "Skip for now" calls skipGoalPicker(). Both close the overlay.
   - Grid reorder: chosen goal ids first (in chosen order), remaining presets after in original order.
   - Reciprocity card: when shouldShowReciprocity(), render a dismissible card between hero and grid: "You've completed {totalSessions()} sessions — nice rhythm. Create a free account to save your progress, or go Premium to keep every feature." with a button to onUpgrade() and an x/dismiss calling dismissReciprocityCard(). Playback must never require an account (do not gate anything on auth).

5. **Library.tsx (new, D-04)**: props { onUpgrade }. Uses useEntitlement() internally.
   - Section "Made for you" (reciprocity framing: "Sessions crafted for you by the Serenade team"): only when configured; if signed out show a gentle note "Sign in on the Premium page to see sessions made for you" with a link-btn to onUpgrade(); if signed in, load `listAssignedAudios()` in useEffect — render assigned (isTemplate false) cards here and template audios (isTemplate true) under a "Templates" sub-heading; each card: name + goalTagline + Play/Stop.
   - Section "My saved sessions": listCustomSessions() from localStorage; per-item Play/Stop and Delete; keep JSON Export (per item, reuse Builder's blob-download approach via exportSessionJSON) and Import (hidden file input -> importSessionJSON -> saveCustomSession -> refresh list) per D-04.
   - Playback: one session at a time via ensureBuilder(); Play calls engine.stop() then engine.start(spec.layers, spec.curve, durationMin) with durationMin from prefs (inf -> null, default 30); poll engine.progress() with a 300ms interval like Builder to show elapsed/remaining and clear playing state when engine stops. When Supabase is not configured, the cloud section is hidden entirely and only "My saved sessions" renders (app stays fully standalone).

6. **Builder.tsx publish panel (D-04)**: after the Save & Share section, when role === "admin" (from useEntitlement) AND isPaymentsConfigured, render a "Publish" section: goal tagline text input; two actions — "Publish as template" (publishAudio(currentSession(), { name, goalTagline, isTemplate: true })) and an assign flow: a select populated from listAllUsers() showing emails + "Publish for this user" button (publishAudio with isTemplate: false, then assignAudio(newId, selectedUserId)); below, list listMyPublishedAudios() with a delete (deleteAudio) per row; flash() notices on success/failure. Panel absent for non-admins and when Supabase is unconfigured — Studio otherwise unchanged.

7. **Upgrade.tsx (D-05 loss aversion + contrast anchoring)**:
   - Anchoring: when period === "annual", render PRICING.IDR.anchorAnnual.price struck through (span with class "price-anchor", CSS line-through + muted color) immediately before the Rp249,000 price; keep the Save 58% pill and the "≈ Rp20,750/month · billed annually" line exactly as-is.
   - Loss aversion: rewrite the PREMIUM bullets from gain to protect/keep framing, e.g. "Keep unlimited session length (∞)", "Keep every goal session — including Boosting Energy, Creative Flow & Power Nap", "Keep the full multi-layer Studio", "Keep custom curves + export/import", "Keep the harmonic frequency finder". Add a personal-stakes block above the plans when totalSessions() >= 1: if currentStreakDays() >= 2 use "You've built a {N}-day streak — keep it alive.", else "You've completed {N} sessions — keep the momentum going." Do not alter checkout flow, LocalActivate, PricingCheckout logic, or amounts.

8. **App.css**: append styles for reco-card, journey strip (bar + steps), goal-picker chips, reciprocity card, streak dots, session-complete card, library sections/cards, publish panel, and .price-anchor — consistent with existing card/chip aesthetics; respect existing CSS variable palette.

Constraints (D-06): no new npm deps; no React imports anywhere in src/audio/**; presets stay the single source of frequency constants; ALL_UNLOCKED and tier gating logic untouched (role gating is orthogonal); payment edge functions and 0001 migration untouched.
  </action>
  <verify>
    <automated>npm run build && npm test && node -e "const{execSync}=require('child_process');const out=execSync('git grep -l \"from .react\" -- src/audio || exit 0').toString().trim();if(out)throw new Error('React import in audio engine: '+out);console.log('audio engine clean')" && node -e "const fs=require('fs');[['src/ui/Upgrade.tsx','anchorAnnual'],['src/App.tsx','recordSessionCompleted'],['src/ui/Home.tsx','recommendedPresetId'],['src/ui/Library.tsx','listAssignedAudios'],['src/ui/Builder.tsx','publishAudio']].forEach(([f,t])=>{if(!fs.readFileSync(f,'utf8').includes(t))throw new Error(f+' missing '+t)});console.log('wiring ok')"</automated>
    <human-check>Run npm run dev: (1) with no VITE_SUPABASE_* env, nav shows Sessions/Library/Science/Premium and Studio only after localStorage.setItem("serenade.role.override","admin") + reload; (2) Home shows Recommended-now card matching current time-of-day, journey strip never 0%, goal picker on first visit reorders grid; (3) complete two short sessions (set a 15-min session, or temporarily verify via recordSessionCompleted in console) -> reciprocity card appears; (4) Upgrade annual shows struck Rp588,000 beside Rp249,000 + streak stakes block; (5) session end shows Session #N + streak dots + come-back copy.</human-check>
  </verify>
  <done>Build + all tests green; nav/Studio/Library role-gated with studio fallback; Library plays cloud (sanitized) and local sessions with import/export; admin publish/assign panel functional against the 0002 schema; Home has recommended card, journey strip, goal picker, reciprocity card; Upgrade shows anchor + keep-framing + stakes block; session-complete view live; no React in src/audio; no new dependencies in package.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase custom_audios.spec | Untrusted jsonb crosses into the audio engine on read |
| authenticated user -> admin-only tables | Role escalation boundary enforced by RLS |
| localStorage -> UI role gating | Local override can flip client-side UI only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a47-01 | Tampering | audioLibrary.listAssignedAudios / listMyPublishedAudios | mitigate | every cloud `spec` passes through exported `sanitizeSession` (clamps Hz 0.5-50, carrier 20-1500, gain 0-1); rows failing sanitization are dropped, never played |
| T-a47-02 | Elevation of privilege | profiles.role | mitigate | no client insert/update/delete policies on profiles; role changes only via service role/SQL; `is_admin()` is security definer with `set search_path = public` (prevents search-path hijack and RLS recursion) |
| T-a47-03 | Information disclosure | profiles emails | mitigate | select policy limited to own row OR is_admin(); listAllUsers works only for admins via RLS |
| T-a47-04 | Spoofing | serenade.role.override localStorage key | accept + mitigate | override honored ONLY when Supabase is unconfigured (no backend data exists to protect); when configured, server profile wins and RLS blocks all data access regardless of client UI state |
| T-a47-05 | Tampering | audio_assignments | mitigate | admin-only insert/update/delete via is_admin() policies; users can only select their own rows |
| T-a47-SC | Tampering | package installs | accept | no new npm/pip/cargo dependencies in this plan (D-06); no install tasks exist |
</threat_model>

<verification>
- `npm run build` (tsc -b && vite build) passes with zero errors.
- `npm test` passes including new tests/progress.test.ts and extended tests/state.test.ts.
- `git grep "from .react" -- src/audio` returns nothing (engine purity, D-06).
- Migration 0002 contains profiles, is_admin(), custom_audios, audio_assignments, RLS enabled on all three tables, and no policies granting client writes to profiles.
- package.json dependencies unchanged (no new deps).
- supabase/functions/** and 0001_payments.sql untouched (git diff shows no changes there).
</verification>

<success_criteria>
- All 6 locked decisions (D-01..D-06) implemented exactly as specified — see Source Coverage Audit.
- A normal user (no account) can: see goal-named presets, tap Recommended-now to start instantly, personalize goals, watch the journey strip fill, complete sessions and see streak dots, and use the Library for saved sessions — all without signing in.
- A signed-in user sees audios assigned to them; an admin can design in Studio and publish/assign them.
- Monetization surfaces (anchor price, keep-framing, stakes block) render without altering payment flow or amounts.
</success_criteria>

<output>
Create `.planning/quick/260707-a47-behavioral-design-upgrade-admin-user-rol/260707-a47-SUMMARY.md` when done.
</output>
