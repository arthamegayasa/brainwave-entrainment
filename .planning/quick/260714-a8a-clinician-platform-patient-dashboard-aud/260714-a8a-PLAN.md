---
phase: quick-260714-a8a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_clinician_platform.sql
  - supabase/functions/create-transaction/index.ts
  - supabase/functions/midtrans-webhook/index.ts
  - src/state/tier.ts
  - src/lib/payments.ts
  - src/lib/roles.ts
  - src/lib/useEntitlement.ts
  - src/lib/clinician.ts
  - src/lib/patientLink.ts
  - src/lib/audioLibrary.ts
  - src/ui/bands.ts
  - src/ui/Dashboard.tsx
  - src/ui/Builder.tsx
  - src/ui/Library.tsx
  - src/ui/Home.tsx
  - src/ui/Upgrade.tsx
  - src/App.tsx
  - src/App.css
  - tests/bands.test.ts
  - tests/pricing.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "A paid clinician-plan checkout activates entitlement tier 'clinician' AND promotes profiles.role user→clinician via the webhook; old 3-part orderIds still settle as premium"
    - "A clinician (or admin) sees Dashboard + Studio in the nav; regular users see neither and fall back to Library"
    - "A clinician can create an invite code, a patient redeems it in Library, and the two are linked (one clinician max per patient)"
    - "A clinician can save a Studio session to their Audio Bank with category + notes, filter the bank by search/category/band, edit metadata, and assign audio to linked patients"
    - "A clinician can hide built-in presets per patient; hidden presets disappear from that patient's Home grid, goal picker, and recommendation"
    - "Upgrade page shows Free / Premium / Clinician cards; premium checkout flow unchanged; clinician tier counts as premium for isPremium"
  artifacts:
    - path: "supabase/migrations/0005_clinician_platform.sql"
      provides: "patient_links, invite_codes, template_visibility, is_clinician(), redeem_invite_code(), get_my_clinician(), policy replacements, custom_audios category/notes"
      min_lines: 120
    - path: "src/lib/clinician.ts"
      provides: "Clinician data layer: patients, invite codes, bank, visibility, assignment"
      exports: ["listMyPatients", "createInviteCode", "listBank", "setPresetHidden", "assignToPatient", "AUDIO_CATEGORIES"]
    - path: "src/lib/patientLink.ts"
      provides: "Patient-side linking: redeem, my clinician, hidden presets"
      exports: ["redeemInviteCode", "getMyClinician", "unlinkMyClinician", "getMyHiddenPresetIds"]
    - path: "src/ui/Dashboard.tsx"
      provides: "Clinician home: Patients tab + Audio Bank tab"
      min_lines: 200
    - path: "tests/pricing.test.ts"
      provides: "Clinician pricing invariants + premium pricing untouched"
  key_links:
    - from: "src/ui/Dashboard.tsx"
      to: "src/lib/clinician.ts"
      via: "imports"
      pattern: "from \"../lib/clinician\""
    - from: "src/ui/Library.tsx"
      to: "redeem_invite_code RPC"
      via: "patientLink.redeemInviteCode"
      pattern: "redeemInviteCode"
    - from: "supabase/functions/midtrans-webhook/index.ts"
      to: "profiles.role"
      via: "service-role update when plan === 'clinician'"
      pattern: "role.*clinician"
    - from: "src/ui/Home.tsx"
      to: "template_visibility"
      via: "getMyHiddenPresetIds in useEffect"
      pattern: "getMyHiddenPresetIds"
    - from: "src/ui/Upgrade.tsx"
      to: "create-transaction edge function"
      via: "createCheckout(plan, period)"
      pattern: "createCheckout\\(.*period\\)"
---

<objective>
Ship the Serenade clinician platform in one pass: 3-role model (user/clinician/admin), invite-code patient linking, per-patient built-in-template curation, a filterable clinician Audio Bank with categories/notes, a clinician Dashboard, and a 3-tier Upgrade page (Free / Premium / Clinician) with Midtrans checkout for the clinician plan — all back-compatible with the live premium billing flow.

Purpose: monetize the professional segment (therapists/clinics) with a Rp249k/mo · Rp1.99M/yr tier where patients listen free.
Output: migration 0005 + extended edge functions + clinician/patient client libs + Dashboard UI + reworked Builder publish panel + 3-tier Upgrade + green `npm run build` and `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/App.tsx
@src/ui/Home.tsx
@src/ui/Library.tsx
@src/ui/Builder.tsx
@src/ui/Upgrade.tsx
@src/ui/bands.ts
@src/audio/presets.ts
@src/state/tier.ts
@src/lib/payments.ts
@src/lib/roles.ts
@src/lib/useEntitlement.ts
@src/lib/audioLibrary.ts
@supabase/migrations/0002_roles_library.sql
@supabase/migrations/0004_harden_grants.sql
@supabase/functions/create-transaction/index.ts
@supabase/functions/midtrans-webhook/index.ts
@tests/state.test.ts

<interfaces>
<!-- Key existing contracts. Use these directly — no codebase exploration needed. -->

From src/lib/useEntitlement.ts (extend, don't rewrite):
```typescript
export interface AuthEntitlementState {
  configured: boolean; loading: boolean; email: string | null;
  entitlement: Entitlement | null; isPremium: boolean; role: Role;
  refresh: () => Promise<void>;
}
// isPremium currently: entitlement?.tier === "premium" && entitlement?.status === "active"
```

From src/lib/payments.ts:
```typescript
export interface Entitlement {
  tier: "free" | "premium";               // widens to include "clinician"
  status: "active" | "inactive" | "pending" | "expired";
  billingPeriod: string | null; currentPeriodEnd: string | null;
}
export async function createCheckout(period: BillingPeriod): Promise<{ token: string; orderId: string }>
// invokes supabase.functions.invoke("create-transaction", { body: { period } })
```

From src/lib/roles.ts:
```typescript
export type Role = "user" | "admin";      // widens to include "clinician"
export async function fetchProfile(): Promise<{ role: Role; email: string | null } | null>
```

From src/lib/audioLibrary.ts:
```typescript
export interface CloudAudio { id: string; name: string; goalTagline: string | null;
  spec: CustomSession; isTemplate: boolean; createdAt: string; }
export async function publishAudio(spec: CustomSession,
  opts: { name: string; goalTagline?: string; isTemplate: boolean }): Promise<string>
export async function listMyPublishedAudios(): Promise<CloudAudio[]>
export async function deleteAudio(audioId: string): Promise<void>
export async function assignAudio(audioId: string, userId: string): Promise<void>  // upsert {audio_id, user_id, assigned_by}
export async function unassignAudio(audioId: string, userId: string): Promise<void>
// Every cloud spec passes through sanitizeSession(row.spec) — rows failing sanitization are DROPPED.
```

From src/audio/builder.ts:
```typescript
export interface BuilderCurve { startHz: number; targetHz: number; endHz: number;
  rampInMin: number; rampOutMin: number; }
export interface CustomSession { id: string; name: string; layers: LayerSpec[]; curve: BuilderCurve; }
```

From src/audio/presets.ts / src/ui/bands.ts:
```typescript
export type Band = "delta" | "theta" | "alpha" | "beta" | "gamma";
export const PRESETS: readonly Preset[];   // 8 presets: id, name, emoji, tagline, band, premium, defaultAmbient
export const BAND_COLORS: Record<Band, string>;   // css var accents
export const BAND_LABELS: Record<Band, string>;   // "Delta · deep sleep" etc.
```

From src/state/tier.ts (PRICING.IDR today — keep untouched, ADD clinician block):
```typescript
monthly: { amount: 49000, price: "Rp49,000", per: "/month" },
annual:  { amount: 249000, price: "Rp249,000", per: "/year", perMonth: "Rp20,750", savePercent: 58 },
anchorAnnual: { amount: 588000, price: "Rp588,000" },
```

Existing DB objects (migrations 0002-0004, already live):
- profiles(user_id PK→auth.users, email, role check in ('user','admin'), display_name, created_at); select policy "read own profile or admin"; NO client write grants (0004 revoked insert/update/delete from authenticated).
- is_admin() security definer stable; EXECUTE revoked from public+anon, kept for authenticated (RLS policies call it).
- custom_audios(id, created_by, name, goal_tagline, spec jsonb, is_template, created_at, updated_at); policies "admin full access" (ALL) + "read templates and assigned" (SELECT, stays).
- audio_assignments(audio_id→custom_audios cascade, user_id→auth.users cascade, assigned_by, created_at, PK(audio_id,user_id)); policies "admin manage assignments" (ALL) + "read own assignments" (SELECT, stays).
- entitlements.tier is plain text, NO check constraint — writing 'clinician' is safe.
- Vitest pattern: tests/*.test.ts with installLocalStorage() Map shim (copy from tests/state.test.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — migration 0005, edge functions, pricing, role plumbing, bandForHz</name>
  <files>supabase/migrations/0005_clinician_platform.sql, supabase/functions/create-transaction/index.ts, supabase/functions/midtrans-webhook/index.ts, src/state/tier.ts, src/lib/payments.ts, src/lib/roles.ts, src/lib/useEntitlement.ts, src/ui/bands.ts, src/ui/Upgrade.tsx, tests/bands.test.ts</files>
  <action>
**1. Create `supabase/migrations/0005_clinician_platform.sql`** implementing D-01 + D-02 exactly. Write file only — do NOT apply it; the orchestrator applies it via Supabase MCP after execution. Contents, in order:

- Widen the role check (D-01): `alter table public.profiles drop constraint if exists profiles_role_check;` then add constraint `profiles_role_check check (role in ('user','clinician','admin'))`.
- `public.is_clinician()` returns boolean, `language sql security definer stable set search_path = public`, body: exists a profiles row where `user_id = auth.uid() and role in ('clinician','admin')`. Mirror the 0004 hardening: `revoke execute on function public.is_clinician() from public, anon;` (authenticated keeps EXECUTE — RLS policies evaluate it as the querying role). Do not touch is_admin().
- `public.patient_links` (clinician_id uuid references auth.users(id) on delete cascade, patient_id uuid references auth.users(id) on delete cascade, created_at timestamptz not null default now(), primary key (clinician_id, patient_id), unique (patient_id)) — the unique enforces at most ONE clinician per patient.
- `public.invite_codes` (code text primary key default `upper(substr(replace(gen_random_uuid()::text,'-',''),1,8))`, clinician_id uuid not null references auth.users(id) on delete cascade, created_at timestamptz not null default now(), expires_at timestamptz not null default `now() + interval '30 days'`, used_by uuid references auth.users(id), used_at timestamptz).
- `public.template_visibility` (clinician_id uuid references auth.users(id) on delete cascade, patient_id uuid references auth.users(id) on delete cascade, preset_id text not null, created_at timestamptz not null default now(), primary key (clinician_id, patient_id, preset_id)). Comment: row present = that built-in preset is HIDDEN for that patient.
- `alter table public.custom_audios add column category text not null default 'other' check (category in ('sleep','meditation','relaxation','anxiety','focus','energy','creativity','other')), add column notes text;`
- RPC `public.redeem_invite_code(invite_code text) returns jsonb`, `language plpgsql security definer set search_path = public`. Logic: normalize `v_code := upper(trim(invite_code))`; if `auth.uid()` is null return `jsonb_build_object('error','not_signed_in')`; select the invite_codes row for v_code — not found → `{error:'invalid_code'}`; `used_by is not null` → `{error:'already_used'}`; `expires_at < now()` → `{error:'expired'}`; caller already has a patient_links row (as patient_id) → `{error:'already_linked'}`; caller IS the code's clinician (self-link guard, Claude's discretion) → `{error:'invalid_code'}`. Otherwise insert into patient_links (clinician_id, patient_id = auth.uid()), update the code's used_by/used_at, look up the clinician's email from profiles, and return `jsonb_build_object('clinician_email', <email>)`. `revoke execute ... from public, anon; grant execute ... to authenticated;`
- RPC `public.get_my_clinician() returns jsonb`, same definer/search_path pattern (required by D-05 — the patient cannot read the clinician's profile under the profiles policy): find the caller's patient_links row, join profiles for the clinician email, return `jsonb_build_object('clinician_email', <email>)`; return null when unlinked. Same revoke/grant as above.
- Enable RLS on all 3 new tables, then policies per D-02:
  - patient_links: "clinician reads own links" + "clinician removes own links" (select/delete using `clinician_id = auth.uid()`); "patient reads own link" + "patient removes own link" (select/delete using `patient_id = auth.uid()`). NO insert policy — only the definer RPC inserts.
  - invite_codes: single ALL-equivalent via separate select/insert/delete policies, each `using`/`with check`: `public.is_clinician() and clinician_id = auth.uid()`.
  - template_visibility: "clinician curates own patients" for ALL, using AND with check: `public.is_clinician() and clinician_id = auth.uid() and exists (select 1 from public.patient_links pl where pl.clinician_id = auth.uid() and pl.patient_id = template_visibility.patient_id)`; plus "patient reads own visibility" for select using `patient_id = auth.uid()`.
- Replace admin-era policies (D-02):
  - custom_audios: `drop policy "admin full access"`; create "clinician manage own" for ALL using/with check `(public.is_clinician() and created_by = auth.uid())`; create "admin all" for ALL using/with check `public.is_admin()`. Existing "read templates and assigned" select policy stays untouched.
  - audio_assignments: `drop policy "admin manage assignments"`; create "clinician assign own audio to own patient" for ALL, using AND with check: `public.is_clinician() and exists (select 1 from public.custom_audios ca where ca.id = audio_id and ca.created_by = auth.uid()) and exists (select 1 from public.patient_links pl where pl.clinician_id = auth.uid() and pl.patient_id = user_id)`; create "admin all assignments" for ALL using/with check `public.is_admin()`. "read own assignments" stays.
  - profiles: `drop policy "read own profile or admin"`; recreate same name for select using `auth.uid() = user_id or public.is_admin() or exists (select 1 from public.patient_links pl where pl.clinician_id = auth.uid() and pl.patient_id = user_id)` — clinicians see ONLY their linked patients' profiles.
- Grants: `revoke all on public.patient_links, public.invite_codes, public.template_visibility from anon;` (authenticated keeps default table privileges — RLS gates rows).

**2. Extend `supabase/functions/create-transaction/index.ts`** (D-03, keep Deno/jsr imports deployable, do NOT deploy): restructure PLANS into a nested matrix `{ premium: { monthly: {amount:49000}, annual: {amount:249000} }, clinician: { monthly: {amount:249000}, annual: {amount:1990000} } }` with labels "Serenade Premium — Monthly", "Serenade Premium — Annual", "Serenade Clinician — Monthly", "Serenade Clinician — Annual". Parse body `{ plan?, period }`: plan defaults to 'premium', reject anything outside 'premium'|'clinician' and 'monthly'|'annual' with the existing `invalid_plan` 400. orderId becomes `${user.id}:${plan}:${period}:${Date.now()}`; item_details id becomes `${plan}-${period}` with the matching label and amount. Keep JWT auth, server-side price, app_config server-key read, and the pending entitlement upsert (tier 'free', status 'pending', billing_period period, provider_ref orderId) unchanged. Update the "prices mirror tier.ts" comment.

**3. Extend `supabase/functions/midtrans-webhook/index.ts`** (D-03): after signature verify + idempotency (unchanged), parse `const parts = orderId.split(":")`. Back-compat: 3 parts = old format `[userId, period, ts]` → plan 'premium'; 4+ parts = `[userId, plan, period, ts]`, coercing an unknown plan value to 'premium'. On paid: entitlements upsert with `tier: plan`, status 'active', billing_period period, current_period_end via existing periodEnd(); ADDITIONALLY when plan === 'clinician' run `admin.from("profiles").update({ role: "clinician" }).eq("user_id", userId).eq("role", "user")` — the role='user' filter guarantees admins are never touched. On deny/cancel/expire: existing tier 'free'/status 'inactive' update, AND ONLY when the failed order's plan is 'clinician': `update profiles set role='user' where user_id = userId and role = 'clinician'`. Pending branch unchanged.

**4. `src/state/tier.ts`** (D-04): keep Tier, ALL_UNLOCKED, TierFeatures, monthly/annual/anchorAnnual all byte-identical. Add a `clinician` block inside PRICING.IDR: `monthly: { amount: 249000, price: "Rp249,000", per: "/month" }`, `annual: { amount: 1990000, price: "Rp1,990,000", per: "/year", perMonth: "Rp165,833", savePercent: 33, anchor: { amount: 2988000, price: "Rp2,988,000" } }` with a comment that the anchor is 12 × clinician monthly, contrast-only, never charged. Do NOT wire clinician into TierFeatures — clinician access is ROLE-gated (D-04).

**5. `src/lib/roles.ts`** (D-01): `export type Role = "user" | "clinician" | "admin"`. fetchProfile coercion becomes: 'admin' → admin, 'clinician' → clinician, anything else → 'user'.

**6. `src/lib/payments.ts`** (D-04/D-05): widen Entitlement.tier to `"free" | "premium" | "clinician"`. Change signature to `createCheckout(plan: "premium" | "clinician", period: BillingPeriod)` sending body `{ plan, period }`.

**7. `src/lib/useEntitlement.ts`** (D-01/D-04): add `isClinician: boolean` to AuthEntitlementState; compute `const isClinician = role === "clinician" || role === "admin"`. isPremium becomes `(entitlement?.tier === "premium" || entitlement?.tier === "clinician") && entitlement?.status === "active"`. Claude's discretion: extend localRoleOverride to also honor the stored value "clinician" (returns 'clinician'), preserving the existing standalone-demo convenience; server profile still wins when configured.

**8. `src/ui/bands.ts`** (D-05): add `export function bandForHz(hz: number): Band` — hz < 4 → 'delta'; hz < 8 → 'theta'; hz < 13 → 'alpha'; hz < 30 → 'beta'; else 'gamma' (matches the documented band mapping; boundaries 4/8/13/30 belong to the higher band).

**9. `src/ui/Upgrade.tsx`** — minimal compile fix ONLY (full redesign is Task 3): change the single `createCheckout(period)` call to `createCheckout("premium", period)`.

**10. `tests/bands.test.ts`** (new): copy the installLocalStorage shim pattern only if needed (bandForHz is pure — likely no shim). Cover: 0.5 and 3.9 → delta; 4 and 7.9 → theta; 8 and 12.9 → alpha; 13 and 29.9 → beta; 30 and 40 → gamma.

Note: supabase/functions/*.ts are Deno files outside the tsc project — `npm run build` does not type-check them; keep `jsr:@supabase/supabase-js@2` imports and Deno.serve intact so they stay deployable.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>Migration 0005 file exists containing patient_links, invite_codes, template_visibility, is_clinician(), redeem_invite_code(), get_my_clinician(), the custom_audios category/notes alter, all policy drops/creates, and anon revokes. Both edge functions handle the plan dimension with 3-part orderId back-compat and clinician role promotion/demotion. PRICING.IDR.clinician exists with the exact D-04 numbers; Role/Entitlement/isClinician plumbing compiles; bandForHz boundary tests pass; `npm run build` and `npm test` green.</done>
</task>

<task type="auto">
  <name>Task 2: Clinician side — clinician.ts lib, Dashboard UI, Builder publish rework, App wiring + styles</name>
  <files>src/lib/clinician.ts, src/lib/audioLibrary.ts, src/ui/Dashboard.tsx, src/ui/Builder.tsx, src/App.tsx, src/App.css</files>
  <action>
**1. New `src/lib/clinician.ts`** (D-05). Every function throws when supabase is null (reuse the `client()` + `currentUserId()` pattern from audioLibrary.ts). Export `const AUDIO_CATEGORIES = ["sleep","meditation","relaxation","anxiety","focus","energy","creativity","other"] as const` and `type AudioCategory = (typeof AUDIO_CATEGORIES)[number]` — single source for Dashboard + Builder pickers. Functions:

- `listMyPatients(): Promise<Array<{patientId: string; email: string | null; linkedAt: string}>>` — IMPORTANT: patient_links FKs point at auth.users, NOT profiles, so PostgREST embedded joins are unavailable. Two-step fetch: select `patient_id, created_at` from patient_links (RLS scopes to own rows), then select `user_id, email` from profiles with `.in("user_id", ids)` (the new profiles policy allows reading linked patients), merge in memory ordered by linkedAt.
- `createInviteCode(): Promise<{code: string; expiresAt: string}>` — insert `{ clinician_id: uid }` into invite_codes, `.select("code, expires_at").single()` (DB defaults generate the code + 30-day expiry).
- `listInviteCodes(): Promise<Array<{code: string; expiresAt: string}>>` — active unused: `.is("used_by", null).gt("expires_at", new Date().toISOString())` ordered by created_at desc.
- `revokeInviteCode(code: string)` — delete by code.
- `unlinkPatient(patientId: string)` — delete patient_links `.eq("patient_id", patientId)` (RLS restricts to own rows).
- `listPatientAssignments(patientId): Promise<Array<{audioId: string; name: string; category: string; goalTagline: string | null}>>` — audio_assignments has a real FK to custom_audios, so use the embedded select `audio_id, custom_audios(name, category, goal_tagline)` with `.eq("user_id", patientId)`.
- `getHiddenPresets(patientId): Promise<string[]>` — template_visibility preset_id list for that patient.
- `setPresetHidden(patientId, presetId, hidden: boolean)` — hidden ? upsert `{clinician_id: uid, patient_id, preset_id}` : delete the matching row.
- `assignToPatient(audioId, patientId)` / `unassignFromPatient(audioId, patientId)` — thin wrappers over the existing assignAudio/unassignAudio upsert/delete semantics (import + re-export or reimplement identically).
- `assignToAllPatients(audioId)` — listMyPatients then one bulk upsert of all `{audio_id, user_id, assigned_by}` rows.
- `listBank(): Promise<BankAudio[]>` — own rows (`created_by = uid`) from custom_audios ordered created_at desc; each spec goes through sanitizeSession and invalid rows are DROPPED (never played); export `interface BankAudio { id; name; goalTagline; category: AudioCategory; notes: string | null; spec: CustomSession; isTemplate: boolean; createdAt: string; band: Band; targetHz: number; layerCount: number }` where `band = bandForHz(spec.curve.targetHz)`, `targetHz = spec.curve.targetHz`, `layerCount = spec.layers.length`. Coerce unknown category values to 'other'.
- `updateAudioMeta(id, patch: {name?: string; goalTagline?: string | null; category?: AudioCategory; notes?: string | null})` — update mapping to name/goal_tagline/category/notes columns.
- Re-export or import `deleteAudio` from audioLibrary for the Dashboard delete action.

**2. `src/lib/audioLibrary.ts`**: publishAudio opts gain optional `category?: string` and `notes?: string` (insert columns category — default 'other' when omitted — and notes). Add optional `category`/`notes` to CloudAudio + toCloudAudio mapping (harmless for Library). Leave listAllUsers in place but nothing outside admin flows may call it after this task (Builder stops using it — clinicians can no longer list all users).

**3. New `src/ui/Dashboard.tsx`** (D-06) — clinician home, gated by the caller (App renders it only for isClinician). `useEntitlement` for context; heading + two tabs "Patients" | "Audio Bank" via simple useState tab switch. Reuse existing class conventions (chips, pill-btn, library-item, notice + flash pattern from Library.tsx).

Patients tab:
- Invite section: "Invite a patient" button → createInviteCode then refresh list; active codes rendered as rows: monospace code text, Copy button (`navigator.clipboard.writeText`, flash "Copied ✓"), expiry date (`toLocaleDateString`), revoke ✕ button. Short explainer under the heading: "Share this code — your patient enters it in their Library."
- Patients list: rows of email + linked date + assigned-count (derive counts via listPatientAssignments lazily on selection, or a lightweight count query — Claude's discretion). Clicking a row selects the patient → detail panel with three blocks:
  [a] "Built-in sessions" — all 8 PRESETS rows (emoji + name) each with a checkbox where CHECKED = VISIBLE. Load getHiddenPresets(patientId) on selection; unchecking calls setPresetHidden(..., true) (inserts the hidden row), checking calls setPresetHidden(..., false).
  [b] "Assigned audio" — rows from listPatientAssignments with an unassign ✕ per row.
  [c] "Add from bank" — a select of listBank items + Assign button (assignToPatient, then refresh block b).
- Soft patient cap: when patients.length >= 30 hide the invite button and show "Patient limit reached (30) — contact us to expand."

Audio Bank tab:
- Filter bar: search text input (name/tagline/notes substring, case-insensitive); category chips (All + the 8 AUDIO_CATEGORIES); band chips (All + Delta/Theta/Alpha/Beta/Gamma using BAND_LABELS). Filters AND together over listBank results held in state.
- Card grid of filtered results: name, goalTagline, category badge, band chip accented with BAND_COLORS[band], a "N layers · target X Hz" line, notes preview (truncate ~80 chars). Per-card actions: "Assign…" (inline patient select populated from listMyPatients, including an "All patients" option → assignToAllPatients, plus confirm button), "Edit" (inline form: name / tagline / category select / notes textarea, save via updateAudioMeta then refresh), "Delete" (window.confirm then deleteAudio).
- Empty state: "Your bank is empty — design a session in the Studio and save it here."

**4. `src/ui/Builder.tsx`** (D-06): change the publish-panel gate from `ent.role === "admin"` to `ent.isClinician` (still AND isPaymentsConfigured). Rework PublishPanel: DELETE the listAllUsers fetch, the user `<select>`, the "Publish for this user" action, and the assignAudio import (assignment now happens in the Dashboard). Keep the tagline input; ADD a category picker over AUDIO_CATEGORIES and a notes input; primary action becomes "Save to Audio Bank" → `publishAudio(session, { name, goalTagline, isTemplate: false, category, notes })` with flash "Saved to your Audio Bank ✓" and a hint line "Assign it to patients from the Dashboard." Keep "Publish as template" visible ONLY when `ent.role === "admin"` (unchanged behavior for admins). Keep the published list (listMyPublishedAudios) + delete + flash() notices. PublishPanel needs the ent value — pass `role`/`isClinician` down as props or read useEntitlement inside; keep the existing refresh pattern.

**5. `src/App.tsx`** (D-06): View union += "dashboard". Nav base stays [Sessions, Library, Science, Premium]; when `ent.isClinician` insert Dashboard + Studio after Library. Studio view gate changes from `role === "admin"` to `ent.isClinician`; add `view === "dashboard"` rendering `<Dashboard />`; when a non-clinician lands on 'dashboard' OR 'studio' render the Library fallback (extend the existing studio fallback pattern and the navCurrent library-highlight logic to cover both). Update the stale D-04 comments to reflect the clinician gate.

**6. `src/App.css`** — append styles reusing existing tokens/classes: dashboard tab bar (active tab state), patient list + selected state + detail panel, invite-code rows (monospace code), checkbox rows for built-in sessions, bank filter bar, bank card grid (grid-template-columns auto-fill minmax pattern like preset-grid), category badge, inline edit form spacing. Keep it lean — reuse chips/pill-btn/library-item wherever possible.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>clinician.ts exports the full D-05 surface (patients, codes, bank, visibility, assignment, AUDIO_CATEGORIES) with the two-step profiles join and sanitizeSession guard; Dashboard renders Patients + Audio Bank tabs with invite codes, per-patient preset checkboxes, assignment management, and a filterable card grid; Builder publish panel is clinician-gated with category/notes + "Save to Audio Bank" and admin-only template button; App nav shows Dashboard + Studio for clinicians with Library fallback for others; build + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Patient side — patientLink lib, Library connect block, Home preset hiding, 3-tier Upgrade, pricing tests</name>
  <files>src/lib/patientLink.ts, src/ui/Library.tsx, src/ui/Home.tsx, src/ui/Upgrade.tsx, src/App.css, tests/pricing.test.ts</files>
  <action>
**1. New `src/lib/patientLink.ts`** (D-05):
- `redeemInviteCode(code: string): Promise<{clinicianEmail: string} | {error: string}>` — throws when supabase null; `supabase.rpc("redeem_invite_code", { invite_code: code })`; map the jsonb result: `{clinician_email}` → success shape, `{error}` passed through.
- `getMyClinician(): Promise<{clinicianEmail: string} | null>` — `rpc("get_my_clinician")`; null when unlinked. (The profiles policy blocks patients reading the clinician's profile — this definer RPC is the only path, per D-05.)
- `unlinkMyClinician(): Promise<void>` — delete own patient_links row (`patient_id = uid`; RLS permits).
- `getMyHiddenPresetIds(): Promise<string[]>` — select preset_id from template_visibility (patient SELECT policy scopes rows). Claude's discretion: this one returns `[]` instead of throwing when supabase is null or the user is signed out, so Home can call it unconditionally without wiring useEntitlement in.

**2. `src/ui/Library.tsx`** (D-06) — add a "My clinician" block, rendered only when `ent.configured`, placed above "Made for you":
- Signed in + not linked (getMyClinician() returned null, fetched in an effect keyed on signedIn): text input "Have a code from your clinician?" + Connect button → redeemInviteCode(trimmed input). Map error keys to friendly copy: invalid_code → "That code doesn't look right — check it and try again."; expired → "This code has expired — ask your clinician for a new one."; already_used → "This code was already used."; already_linked → "You're already connected to a clinician."; not_signed_in → "Please sign in first." Show via the existing flash()/notice pattern or an inline error line.
- Linked: "Connected to {email}" + Disconnect button (window.confirm, then unlinkMyClinician and clear state).
- After a successful redeem: set the linked state AND re-fetch the assigned audios list — refactor the existing listAssignedAudios effect body into a reusable `loadCloud()` callback (or add a refresh-counter dependency) so both the effect and the redeem handler share it. Signed out: show nothing beyond the existing sign-in prompt flow. Playback contracts (onBeforePlay, transport) untouched.

**3. `src/ui/Home.tsx`** (D-06): add `const [hiddenIds, setHiddenIds] = useState<string[]>([])` + one useEffect on mount calling getMyHiddenPresetIds() (returns [] standalone/signed-out — zero behavior change there). Apply:
- `orderedPresets` filtered to exclude hidden ids (after the existing chosen-first ordering).
- GoalPicker: pass the visible presets as a prop (add a `presets: readonly Preset[]` prop to GoalPicker and map over it instead of PRESETS).
- Recommendation: when `recommendedPresetId()` lands on a hidden preset, fall back to the first visible preset (both in the `recommended` render value and inside startRecommended's click-time resolve). Guard the degenerate all-hidden case by falling back to the unfiltered recommendation.
- Touch nothing else: journey, reciprocity, streak, smart-default prefs, SetupSheet all stay as-is (D-07).

**4. `src/ui/Upgrade.tsx`** (D-06) — full 3-tier redesign:
- Lift the billing toggle out of the Premium card to page level (below the header/banners); it drives BOTH paid cards' period.
- Cards: Free (unchanged content) | Premium (featured, "Most popular" badge, existing PREMIUM bullets, anchor strike + subprice logic as today) | Clinician (badge "For professionals", accent class).
- Clinician card pricing from PRICING.IDR.clinician: annual shows anchor strike `clinician.annual.anchor.price` (Rp2,988,000) beside Rp1,990,000/year + subprice "≈ Rp165,833/month · billed annually" (use `clinician.annual.perMonth`); monthly shows Rp249,000/month + "Billed monthly · cancel anytime". Bullets (professional gain framing, D-06): "Everything in Premium", "Patient dashboard — up to 30 patients", "Audio Bank with categories & filters", "Assign custom audio to any patient", "Curate which sessions each patient sees", "Your patients listen free".
- Parametrize PremiumCheckout into `PlanCheckout({ plan, period, ent })` where plan is 'premium'|'clinician': subscribe calls `createCheckout(plan, period)` with the same Snap open/refresh flow, button label uses the matching PRICING block's price; active states: plan 'clinician' shows "Clinician active ✓" when `ent.isClinician` (role clinician/admin); plan 'premium' keeps "Premium active ✓" when ent.isPremium. Sign-in magic-link branch is shared (unchanged). Render PlanCheckout in both paid cards when ent.configured; LocalActivate stays as the unconfigured fallback (Premium card; Claude's discretion whether to note "includes Clinician preview" — keep copy minimal). Keep the early-access banner + stakes-block untouched (D-07).
- Update the save-pill on the annual toggle: it currently shows the premium savePercent (58) — keep as-is (page-level toggle, premium-anchored copy) per D-07's "no breaking changes"; do not invent per-card toggles.

**5. `src/App.css`** — append: `.my-clinician` block styling (bordered library-section variant + connect input row), clinician plan card accent (e.g. distinct border/badge tint via existing color tokens), 3-column plans grid at desktop breakpoint (extend the existing .plans rule; stack on mobile).

**6. New `tests/pricing.test.ts`** (Vitest, import PRICING from src/state/tier and ALL_UNLOCKED/features; use the installLocalStorage shim from tests/state.test.ts for the features() call): assert clinician invariants — clinician.monthly.amount === 249000; clinician.annual.amount === 1990000; anchor.amount === 249000 * 12 === 2988000; `Math.round((1 - 1990000 / 2988000) * 100) === 33` and savePercent === 33; `Math.round(1990000 / 12) === 165833` and perMonth === "Rp165,833". Assert premium untouched (D-07 regression guard): monthly.amount 49000, annual.amount 249000, annual.savePercent 58, anchorAnnual.amount 588000. Assert ALL_UNLOCKED is still true and features().studio true (TierFeatures not wired to clinician).
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>patientLink.ts exposes redeem/getMyClinician/unlink/hidden-ids; Library shows the My clinician connect/disconnect block with friendly error copy and refreshes assigned audios after redeem; Home filters hidden presets from grid, goal picker, and recommendation with safe fallbacks; Upgrade renders 3 cards with page-level billing toggle, clinician anchor pricing, and PlanCheckout('clinician'|'premium') hitting createCheckout(plan, period); pricing invariant tests pass; `npm run build` and `npm test` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase Data API | All table reads/writes cross RLS; client role claims are untrusted |
| Midtrans → midtrans-webhook | Unauthenticated POST; only the SHA-512 signature is trusted |
| patient → redeem_invite_code RPC | Untrusted code string enters a SECURITY DEFINER function |
| cloud spec jsonb → audio engine | Clinician-authored specs are untrusted input to patients' engines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a8a-01 | Elevation | profiles.role | mitigate | Role writes only via webhook service-role update filtered `role='user'`; 0004 already revoked all client write grants on profiles; localRoleOverride is UI-only and ignored when configured |
| T-a8a-02 | Spoofing | midtrans-webhook | mitigate | Existing SHA-512 signature verify before any DB write; unknown plan strings coerce to 'premium' (never 'clinician') |
| T-a8a-03 | Tampering | invite_codes brute force | accept | 8-char code from gen_random_uuid (~4 bits/char over 16 symbols), single-use, 30-day expiry, definer RPC with uniform error shapes; low-value target at current scale |
| T-a8a-04 | Info disclosure | profiles select policy | mitigate | Recreated policy limits clinicians to linked patients only (removes listAllUsers breadth for clinicians); patient reads clinician email only via get_my_clinician definer RPC |
| T-a8a-05 | Tampering | custom_audios.spec → patient playback | mitigate | Existing sanitizeSession gate reused in listBank and listAssignedAudios — invalid specs dropped, never reach the engine |
| T-a8a-06 | Elevation | audio_assignments IDOR | mitigate | ALL policy requires clinician owns the audio (created_by) AND owns the patient link — both existence-checked in the policy per D-02 |
| T-a8a-SC | Tampering | supply chain | mitigate | D-07: zero new npm deps; edge functions keep the already-vetted jsr:@supabase/supabase-js@2 import only |
</threat_model>

<verification>
- `npm run build` (tsc -b + vite build) green after every task.
- `npm test` (vitest run) green: existing suites + new tests/bands.test.ts + tests/pricing.test.ts.
- Migration 0005 contains all D-02 objects (3 tables, 2 RPCs, is_clinician, category/notes alter, policy replacements, anon revokes) — file review, applied later by orchestrator via Supabase MCP.
- Grep sanity: `grep -c "listAllUsers" src/ui/Builder.tsx` returns 0 (Builder no longer lists all users).
- Back-compat: webhook handles a 3-part orderId as premium (code path present and readable in review).
</verification>

<success_criteria>
- All seven locked decisions D-01..D-07 implemented exactly as specified (source audit below).
- Clinician journey works end-to-end in code: buy clinician plan → role promoted → Dashboard visible → invite → patient redeems in Library → clinician curates presets + assigns bank audio → patient's Home/Library reflect it.
- Zero regressions: premium checkout, Library playback contracts (onBeforePlay/nowPlaying), ALL_UNLOCKED, behavioral features (recommendation, journey, goal picker, reciprocity, streak, anchor) untouched except the specified extensions.
- No new npm dependencies; src/audio/** untouched; all copy English.

## Multi-Source Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Multi-clinician patient dashboards | Task 2 (Dashboard.tsx Patients tab) |
| GOAL | Per-patient template curation | Task 1 (template_visibility) + Task 2 (checkboxes) + Task 3 (Home filtering) |
| GOAL | Filterable Audio Bank | Task 2 (listBank + Bank tab filters) |
| GOAL | Invite-code patient linking | Task 1 (invite_codes + RPCs) + Task 2 (invite UI) + Task 3 (Library redeem) |
| GOAL | 3-tier pricing Free/Premium/Clinician | Task 1 (PRICING + edge functions) + Task 3 (Upgrade redesign + tests) |
| CONTEXT | D-01 roles | Task 1 (migration, roles.ts, useEntitlement) — COVERED |
| CONTEXT | D-02 schema/RLS | Task 1 (migration 0005) — COVERED |
| CONTEXT | D-03 edge functions | Task 1 — COVERED (deployment = orchestrator post-execution) |
| CONTEXT | D-04 pricing/tier | Task 1 (tier.ts) + Task 3 (tests) — COVERED |
| CONTEXT | D-05 client lib | Task 1 (payments, bandForHz) + Task 2 (clinician.ts, audioLibrary) + Task 3 (patientLink.ts) — COVERED |
| CONTEXT | D-06 UI | Task 2 (App, Dashboard, Builder, css) + Task 3 (Library, Home, Upgrade, css) — COVERED |
| CONTEXT | D-07 constraints | All tasks (verify gates + regression tests) — COVERED |
</success_criteria>

<output>
Create `.planning/quick/260714-a8a-clinician-platform-patient-dashboard-aud/260714-a8a-SUMMARY.md` when done.

Post-execution (orchestrator, NOT the executor):
- Apply supabase/migrations/0005_clinician_platform.sql to project pwvdobvbwoagvtzqyhfz via Supabase MCP.
- Deploy both edge functions (create-transaction, midtrans-webhook) via Supabase MCP.
- Vercel auto-deploys from GitHub main; no manual deploy step.
No user_setup: Supabase + Midtrans credentials already configured from the previous quick task.
</output>
