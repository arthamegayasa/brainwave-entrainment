---
phase: quick-260714-dc3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/progress.ts
  - src/state/prefs.ts
  - src/ui/Account.tsx
  - src/App.css
  - tests/progress.test.ts
  - src/App.tsx
  - src/ui/Upgrade.tsx
  - src/ui/Library.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "A circular account button sits in the topbar right of the nav: person icon when signed out, uppercase email initial when signed in; clicking it opens the Account sheet, backdrop click closes it"
    - "Signed-out users (configured build) send themselves a magic link from the Account sheet; success shows 'Check your email for a sign-in link.'"
    - "The Premium page contains NO email input anywhere: signed-out paid cards show a single 'Sign in to subscribe' button that opens the Account sheet, and no 'Sign out' links remain on the page"
    - "Signed-in users see profile (avatar + email + plan/role chips), subscription line with 'Manage plan' navigating to the Premium view, clinician connection (role user only), working resets with confirmation, and Sign out"
    - "Library's signed-out 'Made for you' note has a 'Sign in' button opening the Account sheet instead of linking to the Premium page"
    - "resetProgress() and resetPrefs() clear their localStorage keys without throwing; loadProgress() after reset returns fresh state with a new discoveredAt"
    - "npm run build and npm test are green (known pre-existing flake in tests/audio/builder.test.ts 'plays multiple layers simultaneously' ignored if it is the only failure)"
  artifacts:
    - path: "src/ui/Account.tsx"
      provides: "AccountSheet overlay: not-configured note, magic-link sign-in, profile, subscription, my-clinician, settings resets, sign out"
      exports: ["AccountSheet"]
      min_lines: 150
    - path: "src/state/progress.ts"
      provides: "resetProgress() — removes serenade.progress.v1 via storage(), never throws"
      exports: ["resetProgress"]
    - path: "src/state/prefs.ts"
      provides: "resetPrefs() — removes serenade.prefs.v1 via storage(), never throws"
      exports: ["resetPrefs"]
    - path: "tests/progress.test.ts"
      provides: "resetProgress unit tests (clears completions/goals, fresh discoveredAt)"
      contains: "resetProgress"
    - path: "src/App.css"
      provides: ".account-btn circle + .account-section/.account-row + danger button styling (append-only)"
      contains: ".account-btn"
  key_links:
    - from: "src/App.tsx"
      to: "src/ui/Account.tsx"
      via: "import + conditional render on accountOpen"
      pattern: "AccountSheet"
    - from: "src/ui/Account.tsx"
      to: "src/lib/payments.ts"
      via: "signInWithEmail / signOut"
      pattern: "signInWithEmail|signOut"
    - from: "src/ui/Account.tsx"
      to: "src/lib/patientLink.ts"
      via: "getMyClinician / unlinkMyClinician"
      pattern: "unlinkMyClinician"
    - from: "src/ui/Upgrade.tsx"
      to: "Account sheet"
      via: "onSignIn prop (signInWithEmail import REMOVED)"
      pattern: "onSignIn"
    - from: "src/ui/Library.tsx"
      to: "Account sheet"
      via: "onSignIn prop (onUpgrade prop removed)"
      pattern: "onSignIn"
---

<objective>
Standard account entry point: a circular account button in the topbar (person icon signed out, avatar initial signed in) opens an Account sheet containing sign-in (magic link — MOVED here from the Premium page), profile with plan/role chips, subscription with "Manage plan", clinician connection status, settings resets, and sign out. The Premium page loses its embedded email form and Sign out links; Library's signed-out note points at the sheet instead of the Premium page.

Purpose: identity actions live in one predictable place; the Premium page becomes purely a checkout surface.
Output: new src/ui/Account.tsx, resetProgress/resetPrefs state helpers with unit tests, topbar button + wiring in App/Upgrade/Library, appended App.css rules; `npm run build` + `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@src/App.tsx
@src/ui/Upgrade.tsx
@src/ui/Library.tsx
@src/lib/useEntitlement.ts
@src/lib/payments.ts
@src/lib/patientLink.ts
@src/state/progress.ts
@src/state/prefs.ts
@tests/progress.test.ts

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

From src/lib/useEntitlement.ts:
```typescript
export interface AuthEntitlementState {
  configured: boolean;
  loading: boolean;
  email: string | null;
  entitlement: Entitlement | null; // { tier: 'free'|'premium'|'clinician', status, billingPeriod, currentPeriodEnd: string|null }
  isPremium: boolean;
  isClinician: boolean; // true for role clinician AND admin
  role: Role;           // 'user' | 'clinician' | 'admin'
  refresh: () => Promise<void>;
}
export function useEntitlement(): AuthEntitlementState;
```

From src/lib/payments.ts:
```typescript
export async function signInWithEmail(email: string): Promise<void>; // magic link, throws on error
export async function signOut(): Promise<void>;
```

From src/lib/patientLink.ts:
```typescript
export async function getMyClinician(): Promise<{ clinicianEmail: string } | null>; // throws when not configured
export async function unlinkMyClinician(): Promise<void>;
```

Backdrop-close pattern (src/ui/Home.tsx SetupSheet, reuse verbatim):
```tsx
<div className="sheet-backdrop" onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}>
  <div className="sheet" role="dialog" aria-modal="true" aria-label="Account">…</div>
</div>
```

App.tsx facts: `const ent = useEntitlement()` already exists at App level (use it for the topbar button's icon/initial); topbar is `<header className="topbar"><button className="brand">…</button><nav className="topnav">…</nav></header>`; Library is rendered at 3 sites (views library / dashboard-fallback / studio-fallback), Upgrade at 1 site; `goUpgrade = () => setView("upgrade")` exists; Home keeps its `onUpgrade` prop untouched.

Library.tsx facts: `onUpgrade` is used ONLY in the signed-out "Made for you" note (line ~313) → after D-05 the prop is fully unused → remove it. The "My clinician" section in Library (invite-code connect/disconnect, lines ~262-301) is KEPT as-is.

Upgrade.tsx facts: `PlanCheckout` renders for both paid cards when `ent.configured`; its signed-out branch (`!ent.email`) holds the email form to DELETE; "Sign out" link-btns exist in the active branch (~line 241) and the subscribe branch (~line 328). `LocalActivate` (not-configured fallback) is untouched.

State helpers pattern: both progress.ts and prefs.ts have a private `function storage(): Storage | null` with try/catch. localStorage keys: `serenade.progress.v1` (const KEY in progress.ts), `serenade.prefs.v1` (const KEY in prefs.ts).

tests/progress.test.ts shim: `installLocalStorage()` installs a Map-backed localStorage on globalThis in `beforeEach` — it already supports `removeItem`.

App.css tokens/classes available: `--line`, `--line-strong`, `--accent`, `--ink`, `--ink-dim`, `--ink-faint`, `--radius-lg`; `.sheet-backdrop`, `.sheet`, `.sheet-head`, `.chip`, `.chip.small`, `.pill-btn`, `.start-btn.compact`, `.link-btn`, `.text-input`, `.save-row`, `.plan-note`. No `.account-*` classes exist yet (clean namespace).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State reset helpers + AccountSheet component + CSS + unit tests</name>
  <files>src/state/progress.ts, src/state/prefs.ts, src/ui/Account.tsx, src/App.css, tests/progress.test.ts</files>
  <behavior>
    - resetProgress(): after recordSessionCompleted + setChosenGoals, calling resetProgress() then loadProgress() returns completedSessions [], chosenGoals [], goalPickerDone false, and a NEW non-null discoveredAt (loadProgress re-initializes discovery); totalSessions() === 0
    - resetProgress() never throws when localStorage is unavailable (delete globalThis.localStorage shim scenario — mirror the existing resilience describe block style)
    - resetPrefs(): removes serenade.prefs.v1 so loadPrefs() returns DEFAULTS (lastDurationMin 30, visited false) — cover in the same new describe block via loadPrefs/savePrefs imports OR verify localStorage.getItem("serenade.prefs.v1") === null after savePrefs + resetPrefs
  </behavior>
  <action>
    1. **src/state/progress.ts** (per D-03): add at the end
       `export function resetProgress(): void` — body: `const s = storage(); if (!s) return; try { s.removeItem(KEY); } catch { /* storage unavailable */ }`. No other changes.
    2. **src/state/prefs.ts** (per D-03): add identical-shape `export function resetPrefs(): void` using its own `storage()` + `KEY` (`serenade.prefs.v1`). Never throws.
    3. **tests/progress.test.ts** (per D-03): import `resetProgress` from progress and `loadPrefs, savePrefs` plus new `resetPrefs` from `../src/state/prefs`. Add a new `describe("progress: resetProgress / resetPrefs", ...)` with `beforeEach(installLocalStorage)` implementing the behaviors above. For the "new discoveredAt" assertion: capture `loadProgress().discoveredAt`, record sessions/goals, call `resetProgress()`, then assert `loadProgress()` has empty completions/goals AND `discoveredAt` is a non-null string (fresh initialization).
    4. **src/ui/Account.tsx** (per D-02): new file exporting
       `export function AccountSheet({ onClose, onManagePlan, onOpenLibrary }: { onClose: () => void; onManagePlan: () => void; onOpenLibrary: () => void; })`.
       Renders the sheet-backdrop/sheet overlay using the exact backdrop-close pattern from the interfaces block, `aria-label="Account"`, `<h2>Your account</h2>` heading. Calls `useEntitlement()` internally. Local state: `email` (input), `busy`, `msg` (sign-in messages), `note` (settings/disconnect flash note), `clinician` (`{clinicianEmail} | null`). Branches:
       - **NOT configured** (`!ent.configured`): short note "Accounts need the cloud backend — this build runs fully offline." followed by the Settings section (resets still available).
       - **Configured + signed OUT** (`ent.configured && !ent.email`, after `ent.loading` guard showing "Loading…"): value copy "Sign in to sync your plan and receive sessions from your clinician — no password needed."; email `.text-input` + button "Send sign-in link" in a `.save-row` → `signInWithEmail(email.trim())` with busy state; success → `setMsg("Check your email for a sign-in link.")`; catch → `setMsg(error message)`. This form MOVES here from Upgrade (deleted there in Task 2).
       - **Configured + signed IN**, sections in this order (each an `.account-section` with a small heading):
         1. **Profile**: avatar initial (`ent.email[0].toUpperCase()` in an `.account-avatar` circle) + email text + chips row: plan chip — "Premium" | "Clinician" when `ent.entitlement?.status === "active"` and tier is premium/clinician (capitalize the tier), else "Free"; role chip rendered ONLY when `ent.role === "clinician" || ent.role === "admin"` (label = capitalized role). Reuse `.chip` styling (non-interactive: render as `<span className="chip">`).
         2. **Subscription**: `ent.entitlement?.currentPeriodEnd` → line `Active until {new Date(currentPeriodEnd).toLocaleDateString()}`; else "No active subscription — you're on Free." Button "Manage plan" (`.pill-btn`) → `onManagePlan()`.
         3. **My clinician** — ONLY when `ent.role === "user"`. On mount while signed in, `useEffect` fetches `getMyClinician()` into `clinician` (catch → null; include a cancellation guard like Library's). Linked → "Connected to {clinicianEmail}" + "Disconnect" button (danger styling) → `window.confirm("Disconnect from your clinician? Sessions they assigned will no longer appear.")` → `unlinkMyClinician()` → `setClinician(null)` + `void ent.refresh()` + flash note "Disconnected."; catch → flash the error message. Not linked → note "Have an invite code from your clinician? Connect from your Library." + button "Open Library" (`.pill-btn`) → `onOpenLibrary()`.
         4. **Settings**: two danger buttons — "Reset journey & progress" → `window.confirm(...)` → `resetProgress()` → flash "Progress reset."; "Reset preferences" → `window.confirm(...)` → `resetPrefs()` → flash "Preferences reset." Flash = `setNote(text)` rendered as a `.plan-note` inside the sheet (clear any previous note; a `setTimeout` clear ~2.5s is fine but optional — persistent-until-next-action also acceptable).
         5. **"Sign out"** button → `void signOut()` then `onClose()` (useEntitlement's auth listener updates every instance app-wide).
       NOTE: the Settings section renders in BOTH the not-configured and signed-in branches (and NOT in the signed-out branch). All copy English.
    5. **src/App.css** (per D-07 — APPEND ONLY, do not edit existing rules): add a clearly-commented block at the end:
       - `.account-btn`: 36px × 36px circle (`border-radius: 50%`), `border: 1px solid var(--line)`, transparent background, `color: var(--ink-dim)`, grid-centered content, `cursor: pointer`; hover → `border-color: var(--accent); color: var(--ink)`. Avatar-initial variant: 600 weight, ~0.85rem, uppercase (either style the initial inline in `.account-btn` or add `.account-btn.initial`). SVG icon sized ~18px.
       - `.account-section`: top margin ~1.35rem; its heading (`h3` or `.label`-style) small uppercase `var(--ink-faint)` like the existing `.field > .label`.
       - `.account-row`: flex row, gap ~0.6rem, aligned center (avatar + email + chips).
       - `.account-avatar`: ~40px circle, `border: 1px solid var(--line-strong)`, grid-centered initial.
       - Danger styling for reset/disconnect: `.chip.danger` (or `.pill-btn.danger`) — subtle red: e.g. `color: #e58c8c; border-color: rgba(229, 140, 140, 0.4);` hover deepens. Keep it subtle per D-07.
  </action>
  <verify>
    <automated>npm test -- tests/progress.test.ts</automated>
  </verify>
  <done>resetProgress/resetPrefs exported and covered by new passing tests in tests/progress.test.ts; src/ui/Account.tsx compiles (checked fully in Task 2 build) and implements every D-02 branch/section in order; App.css gained the appended .account-* block with no edits to existing rules.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App topbar + strip sign-in from Upgrade + Library sign-in button, full build/test</name>
  <files>src/App.tsx, src/ui/Upgrade.tsx, src/ui/Library.tsx</files>
  <action>
    1. **src/App.tsx** (per D-01 + D-06):
       - `import { AccountSheet } from "./ui/Account";`
       - Add `const [accountOpen, setAccountOpen] = useState(false);`
       - In the topbar `<header>`, AFTER the closing `</nav>`, add the account button:
         `<button className="account-btn" aria-label="Account" onClick={() => setAccountOpen((v) => !v)}>` — content: when `ent.email` is truthy render `ent.email[0].toUpperCase()` (App's existing `ent` instance), else render an inline SVG person icon (`aria-hidden`, simple head-circle + shoulders path, `fill="currentColor"` or stroke, ~18px viewBox). No new deps (D-08).
       - Before the `<footer>`, render:
         `{accountOpen && <AccountSheet onClose={() => setAccountOpen(false)} onManagePlan={() => { setAccountOpen(false); setView("upgrade"); }} onOpenLibrary={() => { setAccountOpen(false); setView("library"); }} />}`
       - Pass `onSignIn={() => setAccountOpen(true)}` to the `<Upgrade />` render site and to ALL THREE `<Library />` render sites (views: library, dashboard fallback, studio fallback). On those three Library sites, REMOVE `onUpgrade={goUpgrade}` (prop deleted in step 3). Home keeps `onUpgrade={goUpgrade}` unchanged — `goUpgrade` stays.
    2. **src/ui/Upgrade.tsx** (per D-04):
       - `export function Upgrade({ onSignIn }: { onSignIn: () => void })` — thread `onSignIn` down to both `<PlanCheckout ... onSignIn={onSignIn} />` call sites (premium + clinician cards); add `onSignIn: () => void` to PlanCheckout's props.
       - PlanCheckout signed-out branch (`!ent.email`): DELETE the entire email form (the `sendLink` closure, `.save-row` with input + chip, and its `msg` rendering). Replace with: `<button className="start-btn compact" onClick={onSignIn}>Sign in to subscribe</button>` + `<p className="plan-note">No password needed — we'll email you a link.</p>`.
       - Remove the now-unused `email` state; keep `busy`/`msg` (still used by the subscribe branch).
       - Remove `signInWithEmail` AND `signOut` from the payments import (keep `createCheckout`); delete BOTH "Sign out" link-btns — in the active branch keep the note as `Signed in as {ent.email}{renews-date-suffix}.` (drop only the trailing Sign out button), and in the subscribe branch keep `Signed in as {ent.email}.` (drop the Sign out button). Identity actions live in Account now.
       - `LocalActivate` and everything else untouched.
    3. **src/ui/Library.tsx** (per D-05):
       - In `LibraryProps` replace `onUpgrade: () => void;` with `onSignIn: () => void;` (onUpgrade becomes unused — its ONLY usage is the signed-out note). Update the destructuring: `Library({ onSignIn, onBeforePlay })`.
       - Replace the signed-out "Made for you" note body: `Sign in on the <link-btn>Premium page</link-btn> to see sessions made for you.` becomes note text "Sign in to see sessions made for you." followed by `<button className="chip small" onClick={onSignIn}>Sign in</button>` (keep it inside the existing `.library-note` paragraph or directly after it — visually consistent, executor's discretion).
       - The "My clinician" section in Library stays EXACTLY as-is (D-02 keeps Library as the invite-code connect surface).
    4. Constraint sweep (D-08): no new npm deps, no supabase/ or edge-function changes, no changes to playback/session logic, all copy English.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>Build green; full test suite green (only allowed failure: pre-existing flake tests/audio/builder.test.ts "plays multiple layers simultaneously" if it is the sole failure). `grep -rn "signInWithEmail" src/ui/Upgrade.tsx` → no matches; `grep -rn "onUpgrade" src/ui/Library.tsx` → no matches; topbar renders .account-btn; AccountSheet opens/closes from App state and both onManagePlan/onOpenLibrary navigate + close.</done>
</task>

</tasks>

<verification>
1. `npm run build` — TypeScript + Vite green (catches prop-contract breaks across App/Upgrade/Library/Account).
2. `npm test` — all suites green including the new resetProgress/resetPrefs tests; ignore only the known builder flake if solitary.
3. Grep gates: `grep -c "AccountSheet" src/App.tsx` ≥ 2 (import + render); `grep -c "onSignIn" src/App.tsx` ≥ 4 (Upgrade + 3 Library sites); `grep -c "signInWithEmail\|signOut" src/ui/Upgrade.tsx` = 0; `grep -c "account-btn" src/App.css` ≥ 1.
4. Manual smoke (post-deploy or `npm run dev`): person icon visible signed out → sheet opens → send-link form present; Premium page paid cards show "Sign in to subscribe" only; Library signed-out note shows Sign in button; signed in → avatar initial, chips, Manage plan → Premium view, resets flash confirmation, Sign out closes sheet and reverts topbar to the person icon.
</verification>

<success_criteria>
- All D-01..D-08 locked decisions implemented exactly (no simplification, no deferral).
- Account sheet is the single identity surface: sign-in form exists ONLY in Account.tsx (moved from Upgrade), sign-out exists ONLY in Account.tsx.
- Upgrade renders zero email inputs and zero sign-out links; Library's signed-out note routes to the sheet; Library's "My clinician" connect block untouched.
- resetProgress/resetPrefs exported, never throw, covered by tests.
- App.css changes are append-only; no behavioral/playback contract touched; no new dependencies.
- `npm run build` + `npm test` green (known builder flake exempt when solitary).
</success_criteria>

<output>
Create `.planning/quick/260714-dc3-account-menu-in-header-sign-in-moved-out/260714-dc3-SUMMARY.md` when done.
</output>
